{{title|operator+,-{{small|(ranges::transform_view::''iterator'')}}}}
{{cpp/ranges/transform_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++20|1=
friend constexpr /*iterator*/ operator+( /*iterator*/ i, difference_type n )
    requires ranges::random_access_range&lt;Base&gt;;
}}
{{dcl|num=2|since=c++20|1=
friend constexpr /*iterator*/ operator+( difference_type n, /*iterator*/ i )
    requires ranges::random_access_range&lt;Base&gt;;
}}
{{dcl|num=3|since=c++20|1=
friend constexpr /*iterator*/ operator-( /*iterator*/ i, difference_type n )
    requires ranges::random_access_range&lt;Base&gt;;
}}
{{dcl|num=4|since=c++20|1=
friend constexpr difference_type operator-( const /*iterator*/&amp; x,
                                            const /*iterator*/&amp; y )
    requires std::sized_sentinel_for&lt;ranges::iterator_t&lt;Base&gt;,
                                     ranges::iterator_t&lt;Base&gt;&gt;;
}}
{{dcl end}}

@1,2@ Returns the iterator {{c|i}} incremented by {{c|n}}.
@3@ Returns the iterator {{c|i}} decremented by {{c|n}}.
@4@ Returns the distance between {{c|x}} and {{c|y}}.

{{cpp/hidden friend|{{tt|transform_view::''iterator''&lt;Const&gt;}}|plural=yes}}

===Parameters===
{{par begin}}
{{par|i, x, y|the iterators.}}
{{par|n|position relative to current location.}}
{{par end}}

===Return value===
Let {{tt|parent_}} denote the pointer to the parent {{tt|transform_view}}, {{tt|current_}} denote the underlying iterator.

@1,2@ {{c|/*iterator*/{*i.parent_, i.current_ + n}&lt;!----&gt;}}
@3@ {{c|/*iterator*/{*i.parent_, i.current_ - n}&lt;!----&gt;}}
@4@ {{c|x.current_ - y.current_}}

===Example===
{{example
|code=
|output=
}}

===Defect reports===
{{dr list begin}}
{{dr list item|wg=lwg|dr=3483|std=C++20|before={{tt|transform_view::iterator}}'s difference is overconstrained|after=requirement is relaxed}}
{{dr list end}}

{{langlinks|de|es|ja|ru|zh}}