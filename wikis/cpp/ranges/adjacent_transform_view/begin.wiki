{{cpp/ranges/adjacent_transform_view/title|begin}}
{{cpp/ranges/adjacent_transform_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++23|1=
constexpr auto begin();
}}
{{dcl|num=2|since=c++23|1=
constexpr auto begin() const
    requires ranges::range&lt;const InnerView&gt; &amp;&amp;
        std::regular_invocable&lt;const F&amp;,
            /*REPEAT*/(ranges::range_reference_t&lt;const V&gt;, N)...&gt;;
}}
{{dcl end}}

Returns an {{rlp|iterator}} to the first element of the {{rlpt|/|adjacent_transform_view}}.

Let {{rlpi|/#Data members|inner_}} be the underlying {{lc|ranges::adjacent_view}}.

@1@ Equivalent to {{c|1=return /*iterator*/&lt;false&gt;(*this, inner_.begin());}}.
@2@ Equivalent to {{c|1=return /*iterator*/&lt;true&gt;(*this, inner_.begin());}}.

===Parameters===
(none)

===Return value===
Iterator to the first element.

===Example===
{{example
|code=
#include &lt;ranges&gt;

int main()
{
    auto sum = [](auto... args) { return (... + args); };

    constexpr auto view = std::views::iota(13, 1337)
                        {{!}} std::views::adjacent_transform&lt;3&gt;(sum);

    static_assert(*view.begin() == 42 and 42 == 13 + 14 + 15);
}
}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/ranges/adaptor/dsc end|adjacent_transform_view}}
{{dsc inc|cpp/ranges/dsc begin}}
{{dsc end}}

{{langlinks|es|ja|ru|zh}}