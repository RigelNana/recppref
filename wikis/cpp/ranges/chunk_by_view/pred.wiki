{{cpp/ranges/chunk_by_view/title|pred}}
{{cpp/ranges/chunk_by_view/navbar}}

{{ddcl|since=c++23|
constexpr const Pred&amp; pred() const;
}}

Returns a reference to the contained {{tt|Pred}} object. Equivalent to {{box|{{c/core|return *}}{{rlpsi|/#pred_}}{{c/core|;}}}}.

The behavior is undefined if {{box|{{rlpsi|/#pred_}}}} does not contain a value.

===Parameters===
(none)

===Return value===
A reference to the contained {{tt|Pred}} object.

===Example===
{{example
|code=
#include &lt;cassert&gt;
#include &lt;concepts&gt;
#include &lt;functional&gt;
#include &lt;initializer_list&gt;
#include &lt;ranges&gt;

int main()
{
    const auto v = {1, 1, 2, 2, 1, 1, 1};
    auto chunks = v {{!}} std::views::chunk_by(std::equal_to{});
    auto pred = chunks.pred();
    static_assert(std::same_as&lt;decltype(pred), std::equal_to&lt;&gt;&gt;);
    assert(pred(v.begin()[0], 1));
}
}}

{{langlinks|es|ja|zh}}