{{cpp/iterator/basic_const_iterator/title|1=operator++,--,+=,-=}}
{{cpp/iterator/basic_const_iterator/navbar}}
{{dcl begin}}
{{dcl|since=c++23|num=1|
constexpr basic_const_iterator&amp; operator++();
}}
{{dcl|since=c++23|num=2|
constexpr void operator++(int);
}}
{{dcl|since=c++23|num=3|
constexpr basic_const_iterator operator++(int)
    requires std::forward_iterator&lt;Iter&gt;;
}}
{{dcl|since=c++23|num=4|
constexpr basic_const_iterator&amp; operator--()
    requires std::bidirectional_iterator&lt;Iter&gt;;
}}
{{dcl|since=c++23|num=5|
constexpr basic_const_iterator operator--(int)
    requires std::bidirectional_iterator&lt;Iter&gt;;
}}
{{dcl|since=c++23|num=6|1=
constexpr basic_const_iterator&amp; operator+=( difference_type n )
    requires std::random_access_iterator&lt;Iter&gt;;
}}
{{dcl|since=c++23|num=7|1=
constexpr basic_const_iterator&amp; operator-=( difference_type n )
    requires std::random_access_iterator&lt;Iter&gt;;
}}
{{dcl end}}

Increments or decrements the iterator, by applying the corresponding operation on the underlying iterator.

@1-3@ Increments by one.
@4,5@ Decrements by one.
@6,7@ Advances the iterator by {{c|n}} or {{c|-n}} positions respectively.

===Return value===
@1,4,6,7@ {{c|*this}}
@3,5@ A copy of {{c|*this}} that was made before the change
@2@ (none)

===Example===
{{example
|code=
|output=
}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/iterator/adaptor/dsc operator_arith|move_iterator}}
{{dsc end}}