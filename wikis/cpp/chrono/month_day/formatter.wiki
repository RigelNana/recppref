{{cpp/title|formatter{{small|&lt;std::chrono::month_day&gt;}}}}
{{cpp/chrono/month_day/navbar}}

{{ddcl|header=chrono|since=c++20|
template&lt; class CharT &gt;
struct formatter&lt;std::chrono::month_day, CharT&gt;;
}}

Specialization of {{lc|std::formatter}} that defines formatting rules for {{lc|std::chrono::month_day}}.

The {{lc|std::formatter}} specialization is usually not directly accessed, but is used through [[cpp/utility/format|formatting functions]].

===Format specification===
{{cpp/chrono/format spec begin}}
{{cpp/chrono/format spec rows/month}}
{{cpp/chrono/format spec rows/day}}
{{cpp/chrono/format spec fail}}
{{cpp/chrono/format spec rows/year}}
{{cpp/chrono/format spec rows/weekday}}
{{cpp/chrono/format spec rows/date}}
{{cpp/chrono/format spec rows/time}}
{{cpp/chrono/format spec rows/count}}
{{cpp/chrono/format spec rows/zone}}
{{cpp/chrono/format spec rows/others}}
{{cpp/chrono/format spec end}}

===Example===
{{example
|code=
#include &lt;chrono&gt;
#include &lt;format&gt;
#include &lt;iostream&gt;

int main()
{
    using namespace std::chrono_literals;
    constexpr std::chrono::month_day md{std::chrono::February / 29d};

    std::cout &lt;&lt; "%B: " &lt;&lt; std::format("{:%B}", md) &lt;&lt; '\n'
              &lt;&lt; "%d: " &lt;&lt; std::format("{:%d}", md) &lt;&lt; '\n';
}
|output=
%B: February
%d: 29
}}

===Defect reports===
{{dr list begin}}
{{dr list item|paper=P2372R3|std=C++20|before={{tt|formatter}} used the global locale or passed locale|after=default {{c/core|"C"}} locale is used when {{ttb|L}} is absent}}
{{dr list end}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/utility/format/dsc format}}
{{dsc end}}

{{langlinks|de|es|ja|ru|zh}}