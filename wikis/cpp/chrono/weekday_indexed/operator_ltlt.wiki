{{cpp/chrono/title|operator&lt;&lt;{{petty|(std::chrono::weekday_indexed)}}}}
{{cpp/chrono/weekday_indexed/navbar}}
{{ddcl|header=chrono|since=c++20|
template&lt; class CharT, class Traits &gt;
std::basic_ostream&lt;CharT, Traits&gt;&amp;
    operator&lt;&lt;( std::basic_ostream&lt;CharT, Traits&gt;&amp; os,
                const std::chrono::weekday_indexed&amp; wdi );
}}

Outputs a textual representation of {{c|wdi}} into the stream {{c|os}}, as if by:

{{cc|1=
if (wdi.index() &gt;=1 &amp;&amp; wdi.index() &lt;= 5)
    os &lt;&lt; std::format(os.getloc(), STATICALLY_WIDEN&lt;CharT&gt;("{:L}[{}]"),
                      wdi.weekday(), wdi.index());
else
    os &lt;&lt; std::format(os.getloc(), STATICALLY_WIDEN&lt;CharT&gt;("{:L}[{} is not a valid index]"),
                      wdi.weekday(), wdi.index());
}}

where {{c|STATICALLY_WIDEN&lt;CharT&gt;("...")}} is {{c|"..."}} if {{tt|CharT}} is {{c|char}}, and {{c|L"..."}} if {{tt|CharT}} is {{c|wchar_t}}.

===Return value===
{{c|os}}

===Defect reports===
{{dr list begin}}
{{dr list item|paper=P2372R3|std=C++20|before=the given locale was used by default|after={{ttb|L}} is needed to use the given locale}}
{{dr list end}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/utility/format/dsc format}}
{{dsc inc|cpp/chrono/dsc formatter|weekday}}
{{dsc end}}

{{langlinks|de|es|ja|ru|zh}}