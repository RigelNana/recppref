{{cpp/chrono/weekday_indexed/title|1=ok}}
{{cpp/chrono/weekday_indexed/navbar}}
{{ddcl|since=c++20|
constexpr bool ok() const noexcept;
}}

Checks if the weekday object and the index stored in {{c|*this}} are both valid.

===Return value===
{{c|true}} if {{c|weekday().ok() {{==}} true}} and {{lc|index()}} is in the range {{closed range|1|5}}. Otherwise {{c|false}}.

===Example===
{{example
|code=
#include &lt;chrono&gt;
#include &lt;iostream&gt;

int main()
{
    std::cout &lt;&lt; std::boolalpha;

    std::chrono::weekday_indexed wdi{std::chrono::Tuesday[2]};
    std::cout &lt;&lt; (wdi.ok()) &lt;&lt; ' ';
    wdi = {std::chrono::weekday(42)[2]}; 
    std::cout &lt;&lt; (wdi.ok()) &lt;&lt; ' ';
    wdi = {std::chrono::Tuesday[-4]}; 
    std::cout &lt;&lt; (wdi.ok()) &lt;&lt; '\n';
}
|output=
true false false
}}

{{langlinks|es|ja|ru|zh}}