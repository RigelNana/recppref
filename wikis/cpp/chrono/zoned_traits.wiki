{{cpp/chrono/title|zoned_traits}}
{{cpp/chrono/navbar}}
{{dcl begin}}
{{dcl header | chrono }}
{{dcl | since=c++20 | num = 1 |
template &lt; class TimeZonePtr &gt;
struct zoned_traits { };
}}
{{dcl | since=c++20 | num = 2 |
template &lt;&gt;
struct zoned_traits&lt;const std::chrono::time_zone*&gt;;
}}
{{dcl end}}

The class {{tt|zoned_traits}} provides a way to customize the behavior of the constructors of {{lc|std::chrono::zoned_time}} with custom time zone pointer types. In particular, it allows such types to specify the default time zone to use and the mapping of a time zone's name to the corresponding time zone pointer. It is acceptable for custom time zone pointer types to not support either operation, in which case the corresponding constructors of {{tt|zoned_time}} will not participate in overload resolution.

The primary template is empty. A specialization is provided for {{c|const std::chrono::time_zone*}}, the default time zone pointer type.

=== Member functions ===
{{member | {{small|std::chrono::zoned_traits&lt;const std::chrono::time_zone*&gt;::}}default_zone |
{{ddcl |
    static const std::chrono::time_zone* default_zone();
}}

Returns a time zone pointer for the default time zone (UTC).

=== Return value ===
{{c|std::chrono::locate_zone("UTC")}}.
}}

{{member | {{small|std::chrono::zoned_traits&lt;const std::chrono::time_zone*&gt;::}}locate_zone|
{{ddcl |
    static const std::chrono::time_zone* locate_zone(std::string_view name);
}}

Returns the time zone pointer for the time zone designated by {{tt|name}}.

=== Return value ===
{{c|std::chrono::locate_zone(name)}}.
}}

{{langlinks|es|ja|ru|zh}}