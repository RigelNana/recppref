{{cpp/title|isxdigit}}
{{cpp/string/byte/navbar}}
{{ddcl|header=cctype|
int isxdigit( int ch );
}}

Checks if the given character is a hexadecimal numeric character ({{tt|0123456789ABCDEFabcdef}}).

The behavior is undefined if the value of {{c|ch}} is not representable as {{c/core|unsigned char}} and is not equal to {{c|EOF}}.

===Parameters===
{{par begin}}
{{par|ch|character to classify}}
{{par end}}

===Return value===
Non-zero value if the character is a hexadecimal numeric character, zero otherwise.

===Notes===
{{lc|std::isdigit}} and {{tt|std::isxdigit}} are the only standard narrow character classification functions that are not affected by the currently installed C locale. although some implementations (e.g. Microsoft in 1252 codepage) may classify additional single-byte characters as digits.
&lt;!-- ref: C standard, note 224 in $7.11.1.1 --&gt;

{{cpp/string/byte/note unsigned char}}

===Example===
{{example
|code=
#include &lt;cctype&gt;
#include &lt;climits&gt;
#include &lt;iostream&gt;
 
int main()
{
    for (int c = 0; UCHAR_MAX &gt;= c; ++c)
        if (isxdigit(c))
            std::cout &lt;&lt; static_cast&lt;char&gt;(c);
    std::cout &lt;&lt; '\n';
}
|output=
0123456789ABCDEFabcdef
}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/locale/dsc isxdigit}}
{{dsc inc|cpp/string/wide/dsc iswxdigit}}
{{dsc see c|c/string/byte/isxdigit}}
{{dsc end}}
{{cpp/string/character classes}}

{{langlinks|de|es|fr|it|ja|pt|ru|zh}}