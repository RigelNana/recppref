{{cpp/title|regex_error}}
{{cpp/regex/regex_error/navbar}}
{{ddcl|header=regex|since=c++11|1=
class regex_error;
}}

Defines the type of exception object thrown to report errors in the regular expressions library.

{{inheritance_diagram/std-regex_error}}

===Member functions===
{{dsc begin}}
{{dsc inc|cpp/regex/regex_error/dsc regex_error}}
{{dsc inc|cpp/regex/regex_error/dsc operator{{=}}}}
{{dsc inc|cpp/regex/regex_error/dsc code}}
{{dsc end}}

{{cpp/error/runtime_error/inherit}}
{{cpp/error/exception/inherit}}

===Example===
{{example
|code=
#include &lt;iostream&gt;
#include &lt;regex&gt;

int main()
{
    try
    {
        std::regex re("[a-b][a");
    }
    catch (const std::regex_error&amp; e)
    {
        std::cout &lt;&lt; "regex_error caught: " &lt;&lt; e.what() &lt;&lt; '\n';
        if (e.code() == std::regex_constants::error_brack)
            std::cout &lt;&lt; "The code was error_brack\n";
    }
}
|output=
regex_error caught: The expression contained mismatched [ and ].
The code was error_brack
|p=true
}}

{{langlinks|de|es|fr|it|ja|pt|ru|zh}}