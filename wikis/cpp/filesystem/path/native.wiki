{{cpp/filesystem/path/title|c_str|native|operator string_type()}}
{{cpp/filesystem/path/navbar}}
{{dcl begin}}
{{dcl|num=1|since=c++17|1=
const value_type* c_str() const noexcept;
}}
{{dcl|num=2|since=c++17|1=
const string_type&amp; native() const noexcept;
}}
{{dcl|num=3|since=c++17|1=
operator string_type() const;
}}
{{dcl end}}

Accesses the native path name as a character string.

@1@ Equivalent to {{c|native().c_str()}}.
@2@ Returns the native-format representation of the pathname by reference.
@3@ Returns the native-format representation of the pathname by value.

===Parameters===
(none)

===Return value===
The native string representation of the pathname, using native syntax, native character type, and native character encoding. This string is suitable for use with OS APIs.

===Notes===
The conversion function {{v|3}} is provided so that APIs that accept {{lc|std::basic_string}} file names can use pathnames with no changes to code.

===Example===
{{cpp/filesystem/path/string_example}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/filesystem/path/dsc string}}
{{dsc inc|cpp/filesystem/path/dsc generic_string}}
{{dsc end}}

{{langlinks|de|es|ja|ru|zh}}