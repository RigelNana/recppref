{{title|operator&lt;&lt;,&gt;&gt;{{small|(std::filesystem::path)}}}}
{{cpp/filesystem/path/navbar}}
{{dcl begin}}
{{dcla|num=1|since=c++17|1=
template&lt; class CharT, class Traits &gt;
friend std::basic_ostream&lt;CharT,Traits&gt;&amp;
    operator&lt;&lt;( std::basic_ostream&lt;CharT,Traits&gt;&amp; os, const path&amp; p );
}}
{{dcla|num=2|since=c++17|1=
template&lt; class CharT, class Traits &gt;
friend std::basic_istream&lt;CharT,Traits&gt;&amp;
    operator&gt;&gt;( std::basic_istream&lt;CharT,Traits&gt;&amp; is, path&amp; p );
}}
{{dcl end}}

Performs stream input or output on the path {{c|p}}. {{c|std::quoted}} is used so that spaces do not cause truncation when later read by stream input operator.

{{cpp/hidden friend|std::filesystem::path|tmpl=yes|plural=yes}} This prevents undesirable conversions in the presence of a {{c|using namespace std::filesystem;}} ''using-directive''.

===Parameters===
{{par begin}}
{{par|os|stream to perform output on}}
{{par|is|stream to perform input on}}
{{par|p|path to insert or extract}}
{{par end}}

===Return value===
@1@ {{c|os}}
@2@ {{c|is}}

{{cpp/impldef exception}}

===Possible implementation===
{{eq impl
|title1=operator&lt;&lt;|ver1=1|1=
template&lt;class CharT, class Traits&gt;
friend std::basic_ostream&lt;CharT,Traits&gt;&amp;
    operator&lt;&lt;(std::basic_ostream&lt;CharT,Traits&gt;&amp; os, const path&amp; p)
{
    os &lt;&lt; std::quoted(p.string&lt;CharT,Traits&gt;());
    return os;
}
|title2=operator&gt;&gt;|ver2=2|2=
template&lt;class CharT, class Traits&gt;
friend std::basic_istream&lt;CharT,Traits&gt;&amp;
    operator&gt;&gt;(std::basic_istream&lt;CharT,Traits&gt;&amp; is, path&amp; p)
{
    std::basic_string&lt;CharT, Traits&gt; t;
    is &gt;&gt; std::quoted(t);
    p = t;
    return is;
}
}}

===Example===
{{example
|code=
#include &lt;filesystem&gt;
#include &lt;iostream&gt;

int main()
{
    std::cout &lt;&lt; std::filesystem::current_path() &lt;&lt; '\n';
    std::cout &lt;&lt; std::filesystem::temp_directory_path() &lt;&lt; '\n';
}
|p=true
|output=
"/home/user"
"/tmp"
}}

===Defect reports===
{{dr list begin}}
{{dr list item|dr=2989|wg=lwg|std=C++17|before=allowed insertion of everything convertible to {{tt|path}} in the presence of a ''using-directive''|after=made hidden friend}}
{{dr list end}}

{{langlinks|de|es|ja|ru|zh}}