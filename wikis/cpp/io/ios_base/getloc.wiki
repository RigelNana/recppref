{{cpp/io/ios_base/title|getloc}}
{{cpp/io/ios_base/navbar}}
{{ddcl|
std::locale getloc() const;
}}

Returns the current locale associated with the stream.

===Parameters===
(none)

===Return value===
The locale object associated with the stream.

===Example===
{{example
|The output shown was obtained using the clang compiler.
|code=
#include &lt;codecvt&gt;
#include &lt;ctime&gt;
#include &lt;iomanip&gt;
#include &lt;iostream&gt;

int main()
{
    std::wbuffer_convert&lt;std::codecvt_utf8&lt;wchar_t&gt;&gt; conv(std::cout.rdbuf());
    std::wostream out(&amp;conv);
    
    out.imbue(std::locale(out.getloc(),
                          new std::time_put_byname&lt;wchar_t&gt;("ja_JP.utf8")));
    
    std::time_t t = std::time(nullptr);
    out &lt;&lt; std::put_time(std::localtime(&amp;t), L"%A %c") &lt;&lt; '\n';
}
|p=true
|output=
木曜日 2023年10月05日 19時47分58秒
}}

===Defect reports===
{{dr list begin}}
{{dr list item|wg=lwg|dr=47|std=C++98|before=the return value was misspecified as the return value of {{tt|imbue()}}|after=corrected}}
{{dr list end}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/io/ios_base/dsc imbue}}
{{dsc end}}

{{langlinks|de|es|fr|it|ja|pt|ru|zh}}