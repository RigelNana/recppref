{{cpp/io/ios_base/title|precision}}
{{cpp/io/ios_base/navbar}}
{{dcl begin}}
{{dcl|num=1|
streamsize precision() const;
}}
{{dcl|num=2|
streamsize precision( streamsize new_precision );
}}
{{dcl end}}

Manages the precision (i.e. how many digits are generated) of floating point output performed by {{lc|std::num_put::do_put}}.

@1@ Returns the current precision. 
@2@ Sets the precision to the given one. Returns the previous precision. 

The default precision, as established by {{lc|std::basic_ios::init}}, is 6.

===Parameters===
{{par begin}}
{{par|new_precision|new precision setting}}
{{par end}}

===Return value===
The precision before the call to the function

===Example===
{{example
|code=
#include &lt;iostream&gt;

int main()
{
    const double d = 12.345678901234;
    std::cout &lt;&lt; "The  default precision is " &lt;&lt; std::cout.precision() &lt;&lt; "\n\n";
    std::cout &lt;&lt; "With default precision d is " &lt;&lt; d &lt;&lt; '\n';
    std::cout.precision(8);
    std::cout &lt;&lt; "With high    precision d is " &lt;&lt; d &lt;&lt; '\n';
}
|output=
The  default precision is 6

With default precision d is 12.3457
With high    precision d is 12.345679
}}

===Defect reports===
{{dr list begin}}
{{dr list item|wg=lwg|dr=189|std=C++98|before='precision' was defined as 'the number of digits after&lt;br&gt;the decimal point', but it is not correct in some cases|after=corrected}}
{{dr list end}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/io/ios_base/dsc width}}
{{dsc inc|cpp/io/manip/dsc setprecision}}
{{dsc end}}

{{langlinks|de|es|fr|it|ja|pt|ru|zh}}