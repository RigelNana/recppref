{{cpp/io/basic_ios/title|fill}}
{{cpp/io/basic_ios/navbar}}
{{dcl begin}}
{{dcl|num=1|
CharT fill() const;
}}
{{dcl|num=2|
CharT fill( CharT ch );
}}
{{dcl end}}

Manages the fill character used to pad the output conversions to the specified field width.

@1@ Returns the current fill character.
@2@ Sets the fill character to {{c|ch}}, returns previous value of the fill character.

===Parameters===
{{par begin}}
{{par|ch|the character to use as fill character}}
{{par end}}

===Return value===
The fill character before the call to the function.

===Example===
{{example
|code=
#include &lt;iomanip&gt;
#include &lt;iostream&gt;

int main ()
{
    std::cout &lt;&lt; "With default setting : [" &lt;&lt; std::setw(10) &lt;&lt; 40 &lt;&lt; "]\n";
    char prev = std::cout.fill('x');
    std::cout &lt;&lt; "Replaced '" &lt;&lt; prev &lt;&lt; "' with '"
              &lt;&lt; std::cout.fill() &lt;&lt; "': [" &lt;&lt; std::setw(10) &lt;&lt; 40 &lt;&lt; "]\n";
}
|output=
With default setting : [        40]
Replaced ' ' with 'x': [xxxxxxxx40]
}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/io/manip/dsc setfill}}
{{dsc end}}

{{langlinks|de|es|fr|it|ja|pt|ru|zh}}