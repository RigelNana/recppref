{{cpp/io/basic_streambuf/title|eback, gptr, egptr}}
{{cpp/io/basic_streambuf/navbar}}
{{dcl begin}}
{{dcl|num=1|
char_type* eback() const;
}}
{{dcl|num=2|
char_type* gptr() const;
}}
{{dcl|num=3|
char_type* egptr() const;
}}
{{dcl end}}

Returns pointers defining the get area.

1) Returns the pointer to the beginning of the get area.

2) Returns the pointer to the current character (''get pointer'') in the get area.

3) Returns the pointer one past the end of the get area.

===Parameters===
(none)

===Return value===
1) The pointer to the beginning of the get area.

2) The pointer to the current character (''get pointer'') in the get area.

3) The pointer one past the end of the get area.

===Notes===
While the names "gptr" and "egptr" refer to the get area, the name "eback" refers to the end of the putback area: stepping backwards from {{tt|gptr}}, characters can be put back until {{tt|eback}}.

===Example===
{{example|
|code=
|output=
}}

===See also===
{{dsc begin}}
{{dsc inc|cpp/io/basic_streambuf/dsc pptr}}
{{dsc end}}

{{langlinks|de|es|fr|it|ja|pt|ru|zh}}