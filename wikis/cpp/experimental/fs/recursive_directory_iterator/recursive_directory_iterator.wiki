{{cpp/experimental/fs/recursive_directory_iterator/title|recursive_directory_iterator}}
{{cpp/experimental/fs/recursive_directory_iterator/navbar}}
{{dcl begin}}
{{dcl|num=1|since=fs_ts|1=
recursive_directory_iterator();
}}
{{dcl|num=2|since=fs_ts|1=
recursive_directory_iterator( const recursive_directory_iterator&amp; ) = default;
}}
{{dcl|num=3|since=fs_ts|1=
recursive_directory_iterator( recursive_directory_iterator&amp;&amp; ) = default;
}}
{{dcl|num=4|since=fs_ts|1=
explicit recursive_directory_iterator( const path&amp; p,
                                       directory_options options = directory_options::none );
}}
{{dcl|num=5|since=fs_ts|1=
recursive_directory_iterator( const path&amp; p,
                              directory_options options, error_code&amp; ec );
}}
{{dcl|num=6|since=fs_ts|1=
recursive_directory_iterator( const path&amp; p, error_code&amp; ec );
}}
{{dcl end}}

Constructs new recursive directory iterator.

@1@ Default constructor. Constructs an end iterator.

@2,3@ Default copy and move constructors.

@4-6@ Constructs an iterator that refers to the first entry in the directory that {{c|p}} resolves to. 

{{todo|errors}}

===Parameters===
{{todo}}

===Exceptions===
@1,5,6@ {{unreviewed noexcept}}

{{todo}}

===Notes===
Recursive directory iterators do not follow directory symlinks by default. To enable this behavior, specify {{tt|directory_options::follow_directory_symlink}} among the {{c|options}} option set.