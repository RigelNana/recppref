{{title|operator&lt;&lt;&lt;small&gt;(std::thread::id)&lt;/small&gt;}}
{{cpp/thread/thread/id/navbar}}
{{dcl begin}}
{{dcl header|thread}}
{{dcl|since=c++11|1=
template&lt; class CharT, class Traits &gt;
std::basic_ostream&lt;CharT,Traits&gt;&amp;
    operator&lt;&lt;( std::basic_ostream&lt;CharT,Traits&gt;&amp; ost, std::thread::id id );
}}
{{dcl end}}

Writes a textual representation of a thread identifier {{c|id}} to the output stream {{c|ost}}. 

If two thread identifiers compare equal, they have identical textual representations; if they do not compare equal, their representations are distinct.

===Parameters===
{{par begin}}
{{par|ost|output stream to insert the data into}}
{{par|id|thread identifier}}
{{par end}} 

===Return value===
{{c|ost}}

{{cpp/impldef exception}}

===Example===
{{example
|code=
#include &lt;chrono&gt;
#include &lt;iostream&gt;
#include &lt;thread&gt;
using namespace std::chrono;

int main()
{
    std::thread t1([]{ std::this_thread::sleep_for(256ms); });
    std::thread t2([]{ std::this_thread::sleep_for(512ms); });

    std::clog &lt;&lt; t1.get_id() &lt;&lt; '\n' &lt;&lt; t2.get_id() &lt;&lt; '\n';

    t1.join();
    t2.join();
}
|p=true
|output=
141592653589793
141421356237309
}}

{{langlinks|de|es|fr|it|ja|pt|ru|zh}}