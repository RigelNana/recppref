{{cpp/title|laguerre|laguerref|laguerrel}}
{{cpp/experimental/special_functions/navbar}}
{{dcl begin}}
{{dcl|num=1|
double      laguerre( unsigned int n, double x );
double      laguerre( unsigned int n, float x );
double      laguerre( unsigned int n, long double x );
float       laguerref( unsigned int n, float x );
long double laguerrel( unsigned int n, long double x );
}}
{{dcl|num=2|
double      laguerre( unsigned int n, IntegralType x );
}}
{{dcl end}}

@1@ 计算实参 {{c|x}} 的 {{c|n} 度无连带[[enwiki:Laguerre_polynomials|拉盖尔多项式]]的值。
@2@ 一组接受任何[[cpp/types/is_integral|整数类型]]实参的重载或函数模板。等价于将实参转型为 {{c|double}} 后的 {{v|1}}。

{{cpp/experimental/special_functions/macro_note|laguerre}}

===参数===
{{par begin}}
{{par|n|多项式的度数，无符号整数类型的值}}
{{par|x|实参，浮点或整数类型的值}}
{{par end}}

===返回值===
如果未发生错误，则返回 {{c|x}} 的无连带拉盖尔多项式的值，即 {{math|{{mfrac|{{mexp|x}}|n!}}{{mfrac|d{{su|p=n}}|dx{{su|p=n}}}}(x{{su|p=n}}{{mexp|-x}})}}。

===错误处理===
根据 [[cpp/numeric/math/math_errhandling|math_errhandling]] 的规定进行错误报告。
* 如果实参为 NaN，则返回 NaN 但不报告定义域错误。
* 如果 {{c|x}} 为负数，则发生定义域错误。
* 如果 {{c|n}} 大于或等于 128，则其行为由实现定义。

===注解===
不支持 TR 29124 但支持 TR 19768 的实现，在头文件 {{tt|tr1/cmath}} 和命名空间 {{tt|std::tr1}} 中提供此函数。

此函数的实现也在 [https://www.boost.org/doc/libs/release/libs/math/doc/html/math_toolkit/sf_poly/laguerre.html boost.math 中可用]。

拉盖尔多项式是方程 {{math|xy{{su|p=,,}} + (1 - x)y{{su|p=,}} + ny {{=}} 0}} 的多项式解。

前几个为：
* laguerre(0, x) {{=}} 1。
* laguerre(1, x) {{=}} {{math|-x + 1}}。
* laguerre(2, x) {{=}} {{math|{{mfrac|1|2}}[x{{su|p=2}} - 4x + 2]}}。
* laguerre(3, x) {{=}} {{math|{{mfrac|1|6}}[-x{{su|p=3}} - 9x{{su|p=2}} - 18x + 6]}}。

===示例===
{{example
|(以 gcc 6.0 运行)
|code=
#define __STDCPP_WANT_MATH_SPEC_FUNCS__ 1
#include &lt;cmath&gt;
#include &lt;iostream&gt;

double L1(double x)
{
    return -x + 1;
}

double L2(double x)
{
    return 0.5 * (x * x - 4 * x + 2);
}

int main()
{
    // spot-checks
    std::cout &lt;&lt; std::laguerre(1, 0.5) &lt;&lt; '=' &lt;&lt; L1(0.5) &lt;&lt; '\n'
              &lt;&lt; std::laguerre(2, 0.5) &lt;&lt; '=' &lt;&lt; L2(0.5) &lt;&lt; '\n';
}
|output=
0.5=0.5
0.125=0.125
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/experimental/special_functions/dsc assoc_laguerre}}
{{dsc end}}

===外部链接===
[https://mathworld.wolfram.com/LaguerrePolynomial.html Weisstein, Eric W. "Laguerre Polynomial."] From MathWorld--A Wolfram Web Resource.

{{langlinks|de|es|fr|it|ja|pt|ru|zh}}