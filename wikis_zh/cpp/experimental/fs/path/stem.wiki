{{cpp/experimental/fs/path/title|stem}}
{{cpp/experimental/fs/path/navbar}}
{{dcl begin}}
{{dcl|since=fs_ts|1=
path stem() const;
}}
{{dcl end}}

返回路径所标识的文件名排除扩展名的部分。

返回从 {{ltt|cpp/experimental/fs/path/filename|filename()}} 的开头直至但不包括最后一个句点（{{ttb|.}}）字符的子字符串。

如果文件名是特殊文件系统组分 {{spar|点}} 或 {{spar|点点}} 之一，或者若它不含有句点，则函数返回整个 {{ltt|cpp/experimental/fs/path/filename|filename()}}。

===参数===
（无）

===返回值===
路径所标识的文件名的主干。

{{cpp/impldef exception}}

===示例===
{{example 
|code=
#include &lt;experimental/filesystem&gt;
#include &lt;iostream&gt;
namespace fs = std::experimental::filesystem;

int main()
{
    std::cout &lt;&lt; fs::path("/foo/bar.txt").stem() &lt;&lt; '\n'
              &lt;&lt; fs::path("/foo/.bar").stem() &lt;&lt; '\n';

    for (fs::path p = "foo.bar.baz.tar"; !p.extension().empty(); p = p.stem())
        std::cout &lt;&lt; p.extension() &lt;&lt; '\n';
}
|output=
 "bar"
""
".tar"
".baz"
".bar"
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/experimental/fs/path/dsc filename}}
{{dsc inc|cpp/experimental/fs/path/dsc extension}}
{{dsc end}}