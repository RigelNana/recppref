{{cpp/experimental/fs/path/title|replace_filename}}
{{cpp/experimental/fs/path/navbar}}
{{dcl begin}}
{{dcl|since=fs_ts|1=
path&amp; replace_filename( const path&amp; replacement );
}}
{{dcl end}}

替换单个文件名组分为 {{c|replacement}}。

等价于以下代码：
{{source|1=
remove_filename();
operator/=(replacement);
}}

如果路径没有文件名组分（{{ltt|cpp/experimental/fs/path/has_path|has_filename}} 返回 {{c|false}}），则其行为未定义。

===参数===
（无）

===返回值===
{{c|*this}}

{{cpp/impldef exception}}

===示例===
{{example
|code=
#include &lt;experimental/filesystem&gt;
#include &lt;iostream&gt;
namespace fs = std::experimental::filesystem;

int main()
{
    std::cout &lt;&lt; fs::path("/foo").replace_filename("bar") &lt;&lt; '\n'
              &lt;&lt; fs::path("/").replace_filename("bar") &lt;&lt; '\n';
}
|output=
"/bar"
"bar"
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/experimental/fs/path/dsc replace_extension}}
{{dsc inc|cpp/experimental/fs/path/dsc filename}}
{{dsc inc|cpp/experimental/fs/path/dsc remove_filename}}
{{dsc mem fun|cpp/experimental/fs/path/has_path|title=has_filename|检查对应路径元素是否非空}}
{{dsc end}}