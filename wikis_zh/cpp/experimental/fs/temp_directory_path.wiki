{{cpp/experimental/fs/title|temp_directory_path}}
{{cpp/experimental/fs/navbar}}
{{dcl begin}}
{{dcl header|experimental/filesystem}}
{{dcl|num=1|since=fs_ts|1=
path temp_directory_path();
path temp_directory_path( error_code&amp; ec );
}}
{{dcl end}}

返回适于临时文件的目录位置。

===参数===
（无）

===返回值===
适于临时文件的目录。该路径保证存在且为目录。接受 {{tt|error_code&amp;}} 实参的重载在发生错误时返回空路径。

===异常===
{{cpp/experimental/fs/error_handling|path to be returned}}

===注解===
在 POSIX 系统上，路径可能为环境变量 {{tt|TMPDIR}}、{{tt|TMP}}、{{tt|TEMP}}、{{tt|TEMPDIR}} 中指定的路径，而当它们均未指定时，返回路径 {{c|"/tmp"}}。

在 Windows 系统上，路径通常为 {{tt|GetTempPath}} 所返回的路径。

===示例===
{{example
|code=
#include &lt;experimental/filesystem&gt;
#include &lt;iostream&gt;
namespace fs = std::experimental::filesystem;

int main()
{
    std::cout &lt;&lt; "临时目录为 " &lt;&lt; fs::temp_directory_path() &lt;&lt; '\n';
}
|p=true
|output=
临时目录为 "C:\Windows\TEMP\"
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/io/c/dsc tmpfile}}
{{dsc end}}