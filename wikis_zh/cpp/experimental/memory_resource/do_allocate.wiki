{{cpp/experimental/pmr/memory_resource/title|do_allocate}}
{{cpp/experimental/lib extensions/pmr/memory_resource/navbar}}
{{ddcl|since=libfund_ts|1=
virtual void* do_allocate( std::size_t bytes, std::size_t alignment ) = 0;
}}

分配大小至少为 {{c|bytes}} 个字节的存储。如果支持指定的对齐 {{c|alignment}}，则所返回的存储按此对齐，否则按 {{c|alignof(std::max_align_t)}} 对齐。

{{c|alignment}} 应当为二的幂。

===异常===
如果无法获取所请求大小和对齐的存储则抛出异常。

===参阅===
{{dsc begin}}
{{dsc inc|cpp/experimental/memory_resource/dsc allocate}}
{{dsc end}}