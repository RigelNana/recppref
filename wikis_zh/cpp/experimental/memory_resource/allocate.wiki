{{cpp/experimental/pmr/memory_resource/title|allocate}}
{{cpp/experimental/lib extensions/pmr/memory_resource/navbar}}
{{ddcl|since=libfund_ts|1=
void* allocate( std::size_t bytes, std::size_t alignment = alignof(std::max_align_t) );
}}

分配具有至少 {{c|bytes}} 个字节的存储。如果支持所指定的对齐 {{c|alignment}}，则所返回的存储按此对齐，否则对齐为 {{c|alignof(std::max_align_t)}}。

等价于 {{c|return do_allocate(bytes, alignment);}}。

===异常===
当无法获取请求的大小和对齐的存储时会抛出异常。

===参阅===
{{dsc begin}}
{{dsc inc|cpp/experimental/memory_resource/dsc do_allocate}}
{{dsc end}}