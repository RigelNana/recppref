{{cpp/experimental/basic_string_view/title | npos}}
{{cpp/experimental/basic_string_view/navbar}}
{{ddcl | since=libfund_ts | 1=
static constexpr size_type npos = size_type(-1);
}}

这是一个特殊值，等于类型 {{tt|size_type}} 所能表示的最大值。确切含义取决于语境，但通常用于在预期视图索引的函数中指定视图末尾，或者由返回视图索引的函数用作错误指示符。