{{cpp/experimental/basic_string_view/title | operator[]}}
{{cpp/experimental/basic_string_view/navbar}}
{{dcl begin}}
{{dcl | since=libfund_ts |
constexpr const_reference operator[](size_type pos) const;
}}
{{dcl end}}

返回到指定位置 {{tt|pos}} 的字符的 const 引用。

不进行边界检查：若 {{c|pos &gt;{{=}} size()}} 则其行为未定义。

===参数===
{{par begin}}
{{par | pos | 要返回字符的位置}}
{{par end}}

===返回值===
到所请求字符的 const 引用

===异常===
不抛出

===复杂度===
常数。

===注解===
与 {{lc|std::basic_string::operator[]}} 不同，{{tt|basic_string_view::operator[](size())}} 有未定义行为而非返回 {{tt|CharT()}}。

===示例===
{{example|code=
#include &lt;iostream&gt;
#include &lt;experimental/string_view&gt;
int main()
{
    std::string str = "Exemplar";
    std::experimental::string_view v = str;
    std::cout &lt;&lt; v[2] &lt;&lt; '\n';
//  v[2] = 'y'; // 错误: 不能通过字符串视图进行修改
    str[2] = 'y';
    std::cout &lt;&lt; v[2] &lt;&lt; '\n';
}
|output=
e
y
}}

===参阅===

{{dsc begin}}
{{dsc inc | cpp/experimental/basic_string_view/dsc at }}
{{dsc end}}