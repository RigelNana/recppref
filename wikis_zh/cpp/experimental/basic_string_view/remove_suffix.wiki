{{cpp/experimental/basic_string_view/title|remove_suffix}}
{{cpp/experimental/basic_string_view/navbar}}
{{dcl begin}}
{{dcl|since=libfund_ts|
constexpr void remove_suffix( size_type n );
}}
{{dcl end}}

将视图末尾向后移动 {{c|n}} 个字符。

若 {{c|n &gt; size()}} 则其行为未定义。

===参数===
{{par begin}}
{{par|n|要从视图末尾移除的字符数量}}
{{par end}}

===返回值===
（无）

===复杂度===
常数。

===示例===
{{example
|code=
#include &lt;experimental/string_view&gt;
#include &lt;iostream&gt;

int main()
{
    char arr[] = {'a', 'b', 'c', 'd', '\0', '\0', '\0'};
    std::experimental::string_view v(arr, sizeof arr);
    auto trim_pos = v.find('\0');
    if (trim_pos != v.npos)
        v.remove_suffix(v.size() - trim_pos);
    std::cout &lt;&lt; "Array: '" &lt;&lt; arr &lt;&lt; "', size=" &lt;&lt; sizeof arr &lt;&lt; '\n'
              &lt;&lt; "View : '" &lt;&lt; v &lt;&lt; "', size=" &lt;&lt; v.size() &lt;&lt; '\n';
}
|output=
Array: 'abcd', size=7
View : 'abcd', size=4
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/experimental/basic_string_view/dsc remove_prefix}}
{{dsc end}}