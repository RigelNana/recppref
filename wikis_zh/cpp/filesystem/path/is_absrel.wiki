{{cpp/filesystem/path/title|is_absolute,is_relative}}
{{cpp/filesystem/path/navbar}}
{{dcl begin}}
{{dcl | num=1 | since=c++17  | 1=
bool is_absolute() const;
}}
{{dcl | num=2 | since=c++17  | 1=
bool is_relative() const;
}}
{{dcl end}}

检查路径是绝对还是相对。绝对路径是无歧义地标识文件位置，无需到另外的起始位置的引用的路径。若原生格式路径为绝对，则首个版本返回 {{c|true}}，否则返回 {{c|false}}；第二版本相反。

===参数===
（无）

===返回值===
@1@ 若 path 为绝对则为 {{c|true}}，否则为 {{c|false}}。

@2@ 若 path 为绝对则为 {{c|false}}，否则为 {{c|true}}。

{{cpp/impldef exception}}

===注解===
路径 {{c|"/"}} 在 POSIX OS 上是绝对的，但在 Windows 上是相对的。

===参阅===
{{dsc begin}}
{{dsc inc | cpp/filesystem/dsc absolute}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}