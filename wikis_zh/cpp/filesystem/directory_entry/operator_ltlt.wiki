{{title|operator&lt;&lt;{{small|(std::filesystem::directory_entry)}}}}
{{cpp/filesystem/directory_entry/navbar}}
{{dcl begin}}
{{dcl|since=c++17|1=
template&lt; class CharT, class Traits &gt;
friend std::basic_ostream&lt;CharT,Traits&gt;&amp;
    operator&lt;&lt;( std::basic_ostream&lt;CharT,Traits&gt;&amp; os, const directory_entry&amp; d );
}}
{{dcl end}}

进行目录条目 {{c|d}} 上的流输出。等价于 {{c|return os &lt;&lt; d.path();}}。

{{cpp/hidden friend|std::filesystem::directory_entry|tmpl=yes}}这阻止 {{c|using namespace std::filesystem;}} ''using 指令''存在时不想要的转换。[[cpp/language/namespace#Using-指令|using-指令]]。

===参数===
{{par begin}}
{{par | os | 要进行输出的流}}
{{par | d | 要插入的 {{tt|directory_entry}} }}
{{par end}}

===返回值===
{{c|os}}

{{cpp/impldef exception}}

===示例===
{{example
|code=
#include &lt;filesystem&gt;
#include &lt;iostream&gt;
namespace fs = std::filesystem;

int main()
{
    const auto entries = {fs::directory_entry{fs::current_path()},
                          fs::directory_entry{fs::temp_directory_path()}&lt;!----&gt;};

    for (const fs::directory_entry&amp; de : entries)
        std::cout &lt;&lt; de &lt;&lt; '\n';
}
|p=true
|output=
"/home/猫"
"/tmp"
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/filesystem/path/dsc operator_ltltgtgt}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}