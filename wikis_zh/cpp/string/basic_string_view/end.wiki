{{cpp/string/basic_string_view/title|end|cend}}
{{cpp/string/basic_string_view/navbar}}
{{dcl begin}}
{{dcl|since=c++17|
constexpr const_iterator end() const noexcept;
}}
{{dcl|since=c++17|
constexpr const_iterator cend() const noexcept;
}}
{{dcl end}}

返回指向视图末字符后一字符的迭代器。此字符表现为占位符，试图访问它会导致未定义行为。

{{image|range-begin-end.svg}}

===参数===
（无）

===返回值===
指向末字符后一字符的 {{tt|const_iterator}}。

===复杂度===
常数。

===示例===
{{example|code=
#include &lt;iostream&gt;
#include &lt;iterator&gt;
#include &lt;string_view&gt;

int main()
{
    constexpr std::string_view str_view("abcd");
    constexpr auto end = str_view.end();
    constexpr auto cend = str_view.cend();

    static_assert
    (
        *std::prev(end) == 'd' &amp;&amp; 'd' == *std::prev(cend) and end == cend
    );
}
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/string/basic_string_view/dsc begin}}
{{dsc inc|cpp/string/basic_string/dsc {{SUBPAGENAMEE}}}}
{{dsc end}}

{{langlinks|en|es|ja}}