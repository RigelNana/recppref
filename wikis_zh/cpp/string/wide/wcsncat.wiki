{{cpp/title| wcsncat}}
{{cpp/string/wide/navbar}}
{{ddcl|header=cwchar|
wchar_t* wcsncat( wchar_t* dest, const wchar_t* src, std::size_t count );
}}

后附来自 {{c|src}} 所指向的宽字符串的至多 {{c|count}} 个宽字符，到 {{c|dest}} 所指向的字符串末尾，若找到空字符则停止。宽字符 {{c|src[0]}} 替换位于 {{c|dest}} 末尾的空终止符。始终后附终止空字符到末尾（故函数可写入的最大宽字符数是 {{c|count + 1}}）。

若目标数组没有对于 {{c|dest}} 和 {{c|src}} 的内容加上终止空宽字符足够的空间，则行为未定义。

若字符串重叠则行为未定义。 

===参数===
{{par begin}}
{{par|dest|指向要后附到的空终止宽字符串的指针}}
{{par|src|指向作为复制来源的宽字符数组的指针}}
{{par|count|要复制的最大宽字符数}}
{{par end}}

===返回值===
{{c|dest}}

===示例===
{{example
|code=
#include &lt;clocale&gt;
#include &lt;cwchar&gt; 
#include &lt;iostream&gt;
 
int main(void) 
{
    wchar_t str[50] = L"Земля, прощай.";
    std::wcsncat(str, L" ", 1);
    std::wcsncat(str, L"В добрый путь.", 8); // 只后附前 8 个宽字符

    std::setlocale(LC_ALL, "en_US.utf8");
    std::wcout.imbue(std::locale("en_US.utf8"));
    std::wcout &lt;&lt; str &lt;&lt; '\n';
}
|p=true
|output=
Земля, прощай. В добрый
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/string/wide/dsc wcscat}}
{{dsc inc|cpp/string/byte/dsc strncat}}
{{dsc inc|cpp/string/wide/dsc wcscpy}}
{{dsc see c|c/string/wide/wcsncat}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}