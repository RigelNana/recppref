{{cpp/string/basic_string/title|front}}
{{cpp/string/basic_string/navbar}}
{{dcl begin}}
{{dcla|num=1|constexpr=c++20|
CharT&amp; front();
}}
{{dcla|num=2|constexpr=c++20|
const CharT&amp; front() const;
}}
{{dcl end}}

返回到字符串中首字符的引用。

{{cpp/hardened ub if|since=c++26| {{lc|empty()}} 是 {{c|true}}}}

===返回值===
{{c|operator[](0)}}

===复杂度===
常数。

===注解===
在 libstdc++ 中，{{tt|front()}} [https://gcc.gnu.org/onlinedocs/libstdc++/manual/strings.html#strings.string.shrink 不能]在 C++98 模式中使用。

===示例===
{{example
|code=
#include &lt;iostream&gt;
#include &lt;string&gt;
 
int main()
{
    std::string s("Exemplary");
    char&amp; f1 = s.front();
    f1 = 'e';
    std::cout &lt;&lt; s &lt;&lt; '\n'; // "exemplary"
    
    std::string const c("Exemplary");
    char const&amp; f2 = c.front();
    std::cout &lt;&lt; &amp;f2 &lt;&lt; '\n'; // "Exemplary"
  }
}
|output=
exemplary
Exemplary
}}

===缺陷报告===
{{dr list begin}}
{{dr list item|wg=lwg|dr=534|std=C++98|before={{lc|std::string}} 没有成员函数 {{tt|front()}}|after=已添加}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/string/basic_string/dsc back}}
{{dsc inc|cpp/string/basic_string_view/dsc {{SUBPAGENAMEE}}}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}