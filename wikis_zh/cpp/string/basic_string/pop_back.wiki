{{cpp/string/basic_string/title|pop_back}}
{{cpp/string/basic_string/navbar}}
{{ddcla|constexpr=c++20|
void pop_back();
}}

从字符串移除末字符。

等价于 {{c|erase(end() - 1)}}。

{{cpp/hardened ub if|since=c++26| {{lc|empty()}} 是 {{c|true}}}}

===复杂度===
常数。

===异常===
不抛出。

===注解===
libstdc++ 中，{{tt|pop_back()}} 在 C++98 模式中[https://gcc.gnu.org/onlinedocs/libstdc++/manual/strings.html#strings.string.shrink 不可用]。

===示例===
{{example
|code=
#include &lt;cassert&gt;
#include &lt;iomanip&gt;
#include &lt;iostream&gt;
#include &lt;string&gt;

int main()
{
    std::string str("Short string!");
    std::cout &lt;&lt; "前：" &lt;&lt; std::quoted(str) &lt;&lt; '\n';
    assert(str.size() == 13);
    
    str.pop_back();
    std::cout &lt;&lt; "后：" &lt;&lt; std::quoted(str) &lt;&lt; '\n';
    assert(str.size() == 12);
    
    str.clear();
//  str.pop_back(); // 未定义行为
}
|output=
前："Short string!"
后："Short string"
}}

===缺陷报告===
{{dr list begin}}
{{dr list item|wg=lwg|dr=534|std=C++98|before={{lc|std::basic_string}} 没有成员函数 {{tt|pop_back()}}|after=已添加}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/string/basic_string/dsc push_back}}
{{dsc inc|cpp/string/basic_string/dsc erase}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}