{{cpp/ranges/subrange/title|size}}
{{cpp/ranges/subrange/navbar}}
{{ddcl|since=c++20|1=
constexpr /*make-unsigned-like-t*/&lt;std::iter_difference_t&lt;I&gt;&gt; size() const
    requires (K == ranges::subrange_kind::sized);
}}

获得 {{rlpt|/|subrange}} 中的元素数：
* 如果 {{box|{{rlpsi|/#StoreSize}}}} 是 {{c|true}}，那么返回 {{box|{{rlpsi|/#size_}}}}。
* 否则返回 {{box|{{lsi|cpp/ranges#to-unsigned-like}}{{sep}}{{c/core|(}}{{rlpsi|/#end_}}{{nbspt}}{{c/core|-}}{{nbspt}}{{rlpsi|/#begin_}}{{sep}}{{c/core|)}}}}。

{{c/core|/*make-unsigned-like-t*/}} 的定义见 {{lsi|cpp/ranges#make-unsigned-like-t}}{{sep}}。

===返回值===
如上所述。

===示例===
{{example
|code=
#include &lt;functional&gt;
#include &lt;iostream&gt;
#include &lt;ranges&gt;
#include &lt;utility&gt;

int main()
{
    const auto v = {2, 2, 2, 7, 1, 1, 1, 1, 8, 2, 2, 2, 2, 2};
    
    // views::chunk_by 的值类型是 ranges::subrange
    
    auto to_pair = [](auto sub) { return std::make_pair(sub[0], sub.size()); };
                                                                 /* ^^^^ */
    auto pairs = v {{!}} std::views::chunk_by(std::equal_to{})
                   {{!}} std::views::transform(to_pair);
    
    for (auto x : pairs bitor std::views::keys)
        std::cout &lt;&lt; x &lt;&lt; ' ';
    std::cout &lt;&lt; '\n';
    for (auto x : pairs bitor std::views::values)
        std::cout &lt;&lt; x &lt;&lt; ' ';
    std::cout &lt;&lt; '\n';
}
|output=
2 7 1 8 2
3 1 4 1 5
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/subrange/dsc empty}}
{{dsc inc|cpp/iterator/dsc size}}
{{dsc inc|cpp/ranges/dsc size}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}