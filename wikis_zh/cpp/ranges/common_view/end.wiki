{{cpp/ranges/common_view/title|end}}
{{cpp/ranges/common_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++20|
constexpr auto end() requires (!/*simple-view*/&lt;V&gt;);
}}
{{dcl|num=2|since=c++20|
constexpr auto end() const requires ranges::range&lt;const V&gt;;
}}
{{dcl end}}

@1@ 返回表示 {{tt|common_view}} 末尾的迭代器，即：
* {{box|{{c/core|ranges::begin(}}{{rlpsi|/#base_}}{{c/core|) + ranges::distance(}}{{rlpsi|/#base_}}{{c/core|)}}}}，若 {{c/core|ranges::random_access_range&lt;V&gt;}} 与 {{c/core|ranges::sized_range&lt;V&gt;}} 均得到满足，
* 否则为 {{box|{{c/core|std::common_iterator&lt;ranges::iterator_t&lt;V&gt;, ranges::sentinel_t&lt;V&gt;&gt;(ranges::end(}}{{rlpsi|/#base_}}{{c/core|))}}}}。

@2@ 同 {{v|1}}，但 {{tt|V}} 为 const 限定。

===返回值===
表示底层视图末尾的迭代器。

===示例===
{{example
|code=
#include &lt;iostream&gt;
#include &lt;numeric&gt;
#include &lt;ranges&gt;

int main()
{
    constexpr int n{4};

    constexpr auto cv1 = std::views::iota(1)
                       {{!}} std::views::take(n)
                       {{!}} std::views::common
                       ;
    constexpr auto cv2 = std::views::iota(2)
                       {{!}} std::views::take(n)
                       ;
    const int product = std::inner_product(cv1.begin(), cv1.end(), 
                                           cv2.begin(),
                                           0);
    std::cout &lt;&lt; product &lt;&lt; '\n';
}
|output=40
}}

===缺陷报告===
{{dr list begin}}
{{dr list item|wg=lwg|dr=4012|std=C++20|before=非 const 重载缺少对简单视图的检查|after=已添加}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/adaptor/dsc begin|common_view}}
{{dsc inc|cpp/ranges/dsc begin}}
{{dsc inc|cpp/ranges/dsc end}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}