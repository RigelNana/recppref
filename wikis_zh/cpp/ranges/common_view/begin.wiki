{{cpp/ranges/common_view/title | begin}}
{{cpp/ranges/common_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++20|
constexpr auto begin() requires (!/*simple_view*/&lt;V&gt;);
}}
{{dcl|num=2|since=c++20|
constexpr auto begin() const requires range&lt;const V&gt;;
}}
{{dcl end}}

@1@ 返回指向 {{tt|common_view}} 首元素的迭代器，即：
* {{box|{{c/core|ranges::begin(}}{{rlpsi|/#base_}}{{c/core|)}}}}，若 {{c/core|ranges::random_access_range&lt;V&gt;}} 与 {{c/core|ranges::sized_range&lt;V&gt;}} 均得到满足，
* 否则为 {{box|{{c/core|std::common_iterator&lt;ranges::iterator_t&lt;V&gt;, ranges::sentinel_t&lt;V&gt;&gt;(ranges::begin(}}{{rlpsi|/#base_}}{{c/core|))}}}}。

@2@ 同 {{v|1}}，但 {{tt|V}} 为 const 限定。

===返回值===
指向底层视图起始的迭代器。

===示例===
{{example
|code=
#include &lt;iostream&gt;
#include &lt;numeric&gt;
#include &lt;ranges&gt;
#include &lt;string_view&gt;

int main()
{
    constexpr auto common = std::views::iota(1)
                          {{!}} std::views::take(3)
                          {{!}} std::views::common
                          ;

    for (int i{}; int e : common)
        std::cout &lt;&lt; (i++ ? " + " : "") &lt;&lt; e;

    std::cout &lt;&lt; " = " &lt;&lt; std::accumulate(common.begin(), common.end(), 0) &lt;&lt; '\n';
}
|output = 1 + 2 + 3 = 6
}}

===缺陷报告===
{{dr list begin}}
{{dr list item|wg=lwg|dr=4012|std=C++20|before=非 const 重载缺少对简单视图的检查|after=已添加}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/adaptor/dsc end|common_view}}
{{dsc inc|cpp/ranges/dsc begin}}
{{dsc inc|cpp/ranges/dsc end}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}