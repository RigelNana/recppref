{{cpp/ranges/transform_view/title|size}}
{{cpp/ranges/transform_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++20|
constexpr auto size() requires ranges::sized_range&lt;V&gt;;
}}
{{dcl|num=2|since=c++20|
constexpr auto size() const requires ranges::sized_range&lt;const V&gt;;
}}
{{dcl end}}

返回元素数。等价于 {{box|{{c/core|ranges::size(}}{{rlpsi|/#base_}}{{c/core|)}}}}。

===返回值===
元素数。

===注解===
若 {{tt|V}} 不实现 {{lconcept|forward_range}}，则 {{tt|size()}} 在调用 {{rlpf|begin}} 后可能非良定义。

===示例===
{{example
|code=
#include &lt;cassert&gt;
#include &lt;cctype&gt;
#include &lt;iostream&gt;
#include &lt;ranges&gt;
#include &lt;string&gt;

int main()
{
    std::string s{"The length of this string is 42 characters"};
    auto to_upper{[](unsigned char c) -&gt; char { return std::toupper(c); }&lt;!----&gt;};
    auto tv{std::ranges::transform_view{s, to_upper}&lt;!----&gt;};
    for (assert(tv.size() == 42); const auto c : tv)
        std::cout &lt;&lt; c;
}
|output=
THE LENGTH OF THIS STRING IS 42 CHARACTERS
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/dsc size}}
{{dsc inc|cpp/ranges/dsc ssize}}
{{dsc end}}

{{langlinks|en|es|ja}}