{{cpp/ranges/adjacent_transform_view/title|adjacent_transform_view}}
{{cpp/ranges/adjacent_transform_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++23|1=
adjacent_transform_view() = default;
}}
{{dcl|num=2|since=c++23|1=
constexpr explicit adjacent_transform_view( V base, F fun );
}}
{{dcl end}}

构造 {{rlpt|/|adjacent_transform_view}}。

@1@ 默认构造函数，[[cpp/language/default_initialization|默认初始化]]底层数据成员 {{rlpi|/#数据成员|fun_}} 与 {{rlpi|/#数据成员|inner_}}。

@2@ 移动构造底层数据成员：{{rlpi|/#数据成员|fun_}} 使用 {{c|std::move(fun)}} 而 {{rlpi|/#数据成员|inner_}} 使用 {{c|std::move(base)}}。

===参数===
{{par begin}}
{{par|base|底层视图}}
{{par|fun|N 元转换函数}}
{{par end}}

===示例===
{{example
|code=
#include &lt;algorithm&gt;
#include &lt;iostream&gt;
#include &lt;iterator&gt;
#include &lt;ranges&gt;

int main()
{
    constexpr static auto v = {1, 2, 3, 4, 5};
    constexpr auto mul = [](auto... x) { return (... * x); };
    constexpr auto view = std::views::adjacent_transform&lt;3&gt;(v, mul);
    std::ranges::copy(view, std::ostream_iterator&lt;int&gt;(std::cout, " "));
    std::cout &lt;&lt; '\n';
}
|output=
6 24 60
}}

{{langlinks|en|es|ja|ru}}