{{cpp/ranges/concat_view/title|concat_view}}
{{cpp/ranges/concat_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++26|1=
concat_view() = default;
}}
{{dcl|num=2|since=c++26|
constexpr concat_view( Views... views );
}}
{{dcl end}}

{|class="wikitable" style="text-align: center;"
!{{nbsp}}重载{{nbsp}}
!style="font-weight: normal"|{{rlpsi|/#views_}}
|-
|{{v|1}}
|[[cpp/language/default initialization|默认初始化]]
|-
|{{v|2}}
|{{nbsp}}以 {{c|std::move(views)...}} 初始化{{nbsp}}
|}

===参数===
{{par begin}}
{{par|views|要适配的各视图对象}}
{{par end}}

===注解===
要调用默认构造函数的情况下必须显式提供 {{tt|Views}}，并且它包含的所有类型都必须可默认初始化。

===示例===
{{example
|这个示例的一个早期预览版本在 [https://godbolt.org/z/fKaKvd3oh Compiler Explorer]。
|code=
#include &lt;algorithm&gt;
#include &lt;ranges&gt;

int main()
{
    using namespace std::ranges;
    
    static constexpr concat_view&lt;empty_view&lt;char&gt;&gt; concat1{}; // 重载 (1)
    static_assert(equal(concat1, views::empty&lt;char&gt;));
    
    static constexpr auto con = {'c', 'o', 'n'};
    static constexpr char cat[]{'c', 'a', 't', '\0'};
    static constexpr auto concat2{views::concat(con, cat)};   // 重载 (2)
    static_assert(equal(concat2, "concat"));
}
}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}