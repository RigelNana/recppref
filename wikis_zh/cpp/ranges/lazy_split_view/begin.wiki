{{cpp/ranges/lazy_split_view/title|begin}}
{{cpp/ranges/lazy_split_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++20|
constexpr auto begin();
}}
{{dcl|num=2|since=c++20|
constexpr auto begin() const
    requires ranges::forward_range&lt;V&gt; &amp;&amp; ranges::forward_range&lt;const V&gt;;
}}
{{dcl end}}

返回指向 {{rlpt|lazy_split_view}} 的首元素的 {{rlpi|outer_iterator}}。

令 {{rlpsi|/#base_}} 为底层视图并另 {{rlpsi|/#current_}} 为底层缓存对象（可能不存在）。

@1@ 等价于
{{source|1=
constexpr auto begin()
{
    if constexpr (ranges::forward_range&lt;V&gt;)
        return /*outer_iterator*/&lt;/*simple_view*/&lt;V&gt;&gt;{*this, ranges::begin(base_)};
    else
    {
        current_ = ranges::begin(base_);
        return /*outer_iterator*/&lt;false&gt;{*this};
    }
}
}}

@2@ 等价于 {{c|1=return /*outer_iterator*/&lt;true&gt;{*this, ranges::begin(base_)};}}。

===返回值===
指向首元素的 {{rlpi|outer_iterator}}。

===示例===
{{example
|code=
|output=
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/adaptor/dsc end|lazy_split_view}}
{{dsc inc|cpp/ranges/dsc begin}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}