{{cpp/ranges/drop_while_view/title|begin}}
{{cpp/ranges/drop_while_view/navbar}}

{{ddcl|since=c++20|
constexpr auto begin();
}}

返回指向视图首元素的迭代器。

相当于返回 {{c|ranges::find_if_not(base_, std::cref(pred()))}}，其中 {{rlpi|/#数据成员|base_}} 为底层视图。若 {{c|*this}} 不存储谓词则行为未定义。

为提供 {{lconcept|range}} 概念所要求的均摊常数时间复杂度，此函数在 {{tt|drop_while_view}} 对象内缓存结果以供后继使用。

===参数===
（无）

===返回值===
指向视图首元素的迭代器。

===示例===
{{example
| code=
#include &lt;cassert&gt;
#include &lt;ranges&gt;

int main()
{
    static constexpr auto data = {0, -1, -2, 3, 1, 4, 1, 5};
    auto view = std::ranges::drop_while_view{data, [](int x){ return x &lt;= 0; }&lt;!----&gt;};
    assert(view.begin()[0] == 3);
}
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/adaptor/dsc end|drop_while_view}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}