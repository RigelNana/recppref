{{cpp/ranges/elements_view/title|end}}
{{cpp/ranges/elements_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++20|1=
constexpr auto end() requires (!/*simple-view*/&lt;V&gt; &amp;&amp; !ranges::common_range&lt;V&gt;);
}}
{{dcl|num=2|since=c++20|1=
constexpr auto end() requires (!/*simple-view*/&lt;V&gt; &amp;&amp; ranges::common_range&lt;V&gt;);
}}
{{dcl|num=3|since=c++20|1=
constexpr auto end() const requires ranges::range&lt;const V&gt;;
}}
{{dcl|num=4|since=c++20|1=
constexpr auto end() const requires ranges::common_range&lt;const V&gt;;
}}
{{dcl end}}

返回表示 {{tt|elements_view}} 末尾的{{rlp|sentinel|哨位}}或{{rlp|iterator|迭代器}}。

令 {{rlpi|/#数据成员|base_}} 为底层视图。等价于：
@1@ {{c|return /*sentinel*/&lt;false&gt;{ranges::end(base_)};}}。
@2@ {{c|return /*iterator*/&lt;false&gt;{ranges::end(base_)};}}。
@3@ {{c|return /*sentinel*/&lt;true&gt;{ranges::end(base_)};}}。
@4@ {{c|return /*iterator*/&lt;true&gt;{ranges::end(base_)};}}。

===参数===
（无）

===返回值===
@1,3@ 比较等于尾迭代器的{{rlp|sentinel|哨位}}。
@2,4@ 指向末元素后一位置的{{rlp|iterator|迭代器}}。

===注解===
{{tt|end()}} 当且仅当底层视图为 {{lconcept|common_range}} 时返回迭代器：{{c/core|elements_view&lt;V,F&gt;}} 凡在 {{tt|V}} 实现 {{lconcept|common_range}} 时实现该概念。

===示例===
{{example
|code=
|output=
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/adaptor/dsc begin|elements_view}}
{{dsc inc|cpp/ranges/dsc end}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}