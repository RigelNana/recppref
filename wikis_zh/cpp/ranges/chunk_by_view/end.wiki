{{cpp/ranges/chunk_by_view/title|end}}
{{cpp/ranges/chunk_by_view/navbar}}
{{ddcl|since=c++23|
constexpr auto end();
}}

返回表示 {{rlpt|/|chunk_by_view}} 的末尾的{{rlp|iterator|迭代器}}或{{lt|cpp/iterator/default_sentinel|哨位}}。

等价于：
{{source|1=
if constexpr (ranges::common_range&lt;V&gt;)
    return /*iterator*/(*this, ranges::end(base_), ranges::end(base_));
else
    return std::default_sentinel;
}}

===参数===
（无）

===返回值===
指向最后一个元素之后的元素的{{rlp|iterator|迭代器}}，或者比较等于尾迭代器的哨位。

===注解===
当且仅当底层视图是 {{lconcept|common_range}} 时，{{tt|end()}} 返回迭代器：只要 {{c|V}} 实现 {{lconcept|common_range}}，那么 {{c|chunk_by_view&lt;V,Pred&gt;}} 也实现它。

===示例===
{{example
|code=
|output=
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/adaptor/dsc begin|chunk_by_view}}
{{dsc inc|cpp/ranges/dsc end}}
{{dsc end}}

{{langlinks|de|en|es|ja|ru}}