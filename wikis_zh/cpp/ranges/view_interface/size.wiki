{{cpp/ranges/view_interface/title|size}}
{{cpp/ranges/view_interface/navbar}}
{{dcl begin}}
{{dcl|num=1|since=c++20|
constexpr auto size() requires ranges::forward_range&lt;D&gt; &amp;&amp;
    std::sized_sentinel_for&lt;ranges::sentinel_t&lt;D&gt;,
                            ranges::iterator_t&lt;D&gt;&gt;;
}}
{{dcl|num=2|since=c++20|
constexpr auto size() const requires ranges::forward_range&lt;const D&gt; &amp;&amp;
    std::sized_sentinel_for&lt;ranges::iterator_t&lt;const D&gt;,
                            ranges::sentinel_t&lt;const D&gt;&gt;;
}}
{{dcl end}}

{{tt|size()}} 成员函数的默认实现通过计算哨位与起始迭代器间的差获得范围的大小。

===返回值===
@1@ {{box|{{lsi|cpp/ranges#to-unsigned-like}}{{sep}}{{c/core|(ranges::end(static_cast&lt;D&amp;&gt;(this)) -}}&lt;br&gt;{{nbspt|21}}{{c/core|ranges::begin(static_cast&lt;D&amp;&gt;(this)))}}}}。

@2@ {{box|{{lsi|cpp/ranges#to-unsigned-like}}{{sep}}{{c/core|(ranges::end(static_cast&lt;const D&amp;&gt;(this)) -}}&lt;br&gt;{{nbspt|21}}{{c/core|ranges::begin(static_cast&lt;const D&amp;&gt;(this)))}}}}。

===注解===
下列派生类型能使用 {{tt|size()}} 的默认实现：
* {{lc|std::ranges::drop_while_view}}

下列类型派生自 {{lc|ranges::view_interface|std::ranges::view_interface}} 并且不声明其自身的 {{tt|size()}} 成员函数，但它们无法使用默认实现，因为其迭代器与哨位类型决不满足 {{lconcept|sized_sentinel_for}}：
* {{lc|ranges::basic_istream_view|std::ranges::basic_istream_view}}
* {{lc|std::ranges::filter_view}}
* {{lc|ranges::join_view|std::ranges::join_view}}
* {{lc|ranges::lazy_split_view|std::ranges::lazy_split_view}}
* {{lc|ranges::split_view|std::ranges::split_view}}
* {{lc|std::ranges::take_while_view}}

===缺陷报告===
{{dr list begin}}
{{dr list item|wg=lwg|dr=3646|std=C++20|before={{tt|size}} 函数的默认实现返回有符号类型|after=返回无符号类型}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/iterator/dsc size}}
{{dsc inc|cpp/ranges/dsc size}}
{{dsc inc|cpp/ranges/dsc ssize}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}