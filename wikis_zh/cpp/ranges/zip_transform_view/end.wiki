{{cpp/ranges/zip_transform_view/title | end}}
{{cpp/ranges/zip_transform_view/navbar}}
{{dcl begin}}
{{dcl | num=1 | since=c++23 |
constexpr auto end();
}}
{{dcl | num=2 | since=c++23 |
constexpr auto end() const
    requires ranges::range&lt;const /*InnerView*/&gt; &amp;&amp;
             std::regular_invocable&lt;const F&amp;,
                 ranges::range_reference_t&lt;const Views&gt;...&gt;;
}}
{{dcl end}}

返回与 {{rlpt|/|zip_transform_view}} 的尾迭代器比较相等的{{rlp|iterator|迭代器}}或{{rlp|sentinel|哨位}}。

令 {{tt|zip_}} 代表底层的视图元组。

@1@ 等价于：&lt;br&gt;{{c|
if constexpr (ranges::common_range&lt;/*InnerView*/&gt;)
    return /*iterator*/&lt;false&gt;(*this, zip_.end());
else
    return /*sentinel*/&lt;false&gt;(zip_.end());
}}

@2@ 等价于：&lt;br&gt;{{c|
if constexpr (ranges::common_range&lt;const /*InnerView*/&gt;)
    return /*iterator*/&lt;true&gt;(*this, zip_.end());
else
    return /*sentinel*/&lt;true&gt;(zip_.end());
}}

===参数===
（无）

===返回值===
表示 {{tt|zip_transform_view}} 的末尾的迭代器或哨位，如上所述。

===示例===
{{example
|code=
|output=
}}

===参阅===
{{dsc begin}}
{{dsc inc | cpp/ranges/adaptor/dsc begin | zip_transform_view}}
{{dsc inc | cpp/ranges/dsc end}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}