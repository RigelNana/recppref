{{cpp/ranges/stride_view/title|stride}}
{{cpp/ranges/stride_view/navbar}}
{{ddcl|since=c++23|constexpr ranges::range_difference_t&lt;_Vp&gt; stride() const noexcept;
}}

返回步长对象 {{rlpsi|/#stride_}} 的副本。等价于 {{box|{{c/core|return}}{{nbspt}}{{rlpsi|/#stride_}}{{c/core|;}}}}。

===返回值===
步长值。

===示例===
{{example
|code=
#include &lt;ranges&gt;

int main()
{
    constexpr auto view = std::views::iota(1337)
                        {{!}} std::views::stride(42);
    static_assert(view.stride() == 42);
}
}}

{{langlinks|en|es|ja|ru}}