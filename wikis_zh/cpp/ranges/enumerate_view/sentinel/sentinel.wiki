{{cpp/ranges/enumerate_view/sentinel/title|''sentinel''}}
{{cpp/ranges/enumerate_view/navbar}}

{{dcl begin}}
{{dcl|num=1|since=c++23|1=
/*sentinel*/() = default;
}}
{{dcl|num=2|since=c++23|1=
constexpr /*sentinel*/( /*sentinel*/&lt;!Const&gt; i )
    requires Const &amp;&amp;
        std::convertible_to&lt;ranges::sentinel_t&lt;V&gt;, ranges::sentinel_t&lt;Base&gt;&gt;;
}}
{{dcl|num=3|notes={{mark expos}}|1=
private:
constexpr explicit /*sentinel*/( ranges::sentinel_t&lt;Base&gt; end );
}}
{{dcl end}}

构造{{rlp|/|哨位}}。

@1@ 默认构造函数。[[cpp/language/value_initialization|值初始化]]底层哨位为 {{c|ranges::sentinel_t&lt;Base&gt;()}}。
@2@ 从 {{c|/*sentinel*/&lt;false&gt;}} 转换为 {{c|/*sentinel*/&lt;true&gt;}}。以 {{c|std::move(other.end_)}} 移动构造底层哨位 {{rlpi|/#数据成员|end_}}。
@3@ {{lc|enumerate_view::end}} 所使用的私有构造函数。以 {{c|std::move(end)}} 移动构造 {{rlpi|/#数据成员|end_}}。用户不可访问此构造函数。

===参数===
{{par begin}}
{{par|i|a {{c|/*sentinel*/&lt;false&gt;}}}}
{{par end}}

===示例===
{{example
|code=
|output=
}}

{{langlinks|de|en|es|ja|ru}}