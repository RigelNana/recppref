{{cpp/ranges/iota_view/title|empty}}
{{cpp/ranges/iota_view/navbar}}
{{ddcl|since=c++20|
constexpr bool empty() const;
}}

检查范围是否为空（即起始值是否等于哨位值）。

===返回值===
{{box|{{rlpsi|/#value_}}{{nbspt}}{{c/core|1===}}{{nbspt}}{{rlpsi|/#bound_}}}}

===示例===
{{example
|code=
#include &lt;cassert&gt;
#include &lt;ranges&gt;

int main()
{
    auto a = std::ranges::iota_view&lt;int, int&gt;();
    assert(a.empty());
    
    auto b = std::ranges::iota_view(4);
    assert(!b.empty());
    
    auto c = std::ranges::iota_view(4, 8);
    assert(!c.empty());
}
}}

===缺陷报告===
{{dr list begin}}
{{dr list item|wg=lwg|dr=4001|std=C++20|before=继承的成员函数 {{tt|empty}} 不一定合法|after=始终提供 {{tt|empty}}}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/adaptor/dsc size|iota_view}}
{{dsc end}}

{{langlinks|de|en|es|ja|ru}}