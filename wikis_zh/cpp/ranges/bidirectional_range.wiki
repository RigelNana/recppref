{{cpp/ranges/title|bidirectional_range}}
{{cpp/ranges/navbar}}

{{ddcl|header = ranges|since=c++20|1=
template&lt; class T &gt;
concept bidirectional_range =
    ranges::forward_range&lt;T&gt; &amp;&amp; std::bidirectional_iterator&lt;ranges::iterator_t&lt;T&gt;&gt;;

}}

{{tt|bidirectional_range}} 概念是 {{lconcept|range}} 的细化，{{lc|ranges::begin}} 对它返回 {{lconcept|bidirectional_iterator}} 的实现。

===示例===
{{example
|code=
#include &lt;forward_list&gt;
#include &lt;list&gt;
#include &lt;ranges&gt;
#include &lt;set&gt;
#include &lt;unordered_set&gt;

int main()
{
    static_assert(
            std::ranges::bidirectional_range&lt;std::set&lt;int&gt;&gt; and
        not std::ranges::bidirectional_range&lt;std::unordered_set&lt;int&gt;&gt; and
            std::ranges::bidirectional_range&lt;std::list&lt;int&gt;&gt; and
        not std::ranges::bidirectional_range&lt;std::forward_list&lt;int&gt;&gt;
    );
}
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/dsc forward_range}}
{{dsc inc|cpp/ranges/dsc random_access_range}}
{{dsc end}}

{{langlinks|es|en|ja}}