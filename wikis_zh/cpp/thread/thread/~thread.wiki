{{cpp/thread/thread/title|~thread}}
{{cpp/thread/thread/navbar}}
{{ddcl|since=c++11|
~thread();
}}

销毁线程对象。

若 {{c|*this}} 拥有关联线程（{{c|1=joinable() == true}}），则调用 {{c|std::terminate()}}。

===注解===
在下列操作后线程对象无关联的线程（从而可安全销毁）
* 被默认构造
* 被移动
* 已调用 {{lc|join()}}
* 已调用 {{lc|detach()}}

===示例===
{{example
|code=
#include &lt;thread&gt;
using namespace std::chrono_literals;

int main()
{
    auto bleah = std::thread{[]{ std::this_thread::sleep_for(13ms); }&lt;!----&gt;};

}   // ~thread 调用 std::terminate()
|p=true
|output=
terminate called without an active exception
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/thread/jthread/dsc destructor}}
{{dsc end}}

{{langlinks|ar|de|en|es|fr|it|ja|ko|pt|ru}}