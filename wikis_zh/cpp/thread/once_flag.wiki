{{cpp/title|once_flag}}
{{cpp/thread/navbar}}
{{ddcl | header=mutex | since=c++11 |
class once_flag;
}}

类 {{tt|std::once_flag}} 是 {{lc|std::call_once}} 的辅助结构体。

传递给多次 {{lc|std::call_once}} 调用的 {{tt|std::once_flag}} 类型的对象，允许这些调用彼此协调，从而令只有一次调用实际完成运行。

{{tt|std::once_flag}} 既不可复制亦不可移动。

===成员函数===
{{member | {{small|std::once_flag::}}once_flag |
{{ddcl |
constexpr once_flag() noexcept;
}}

构造 {{tt|once_flag}} 对象。设置内部状态为指示尚未调用函数。

===参数===
（无）

}}

===参阅===
{{dsc begin}}
{{dsc inc | cpp/thread/dsc call_once}}
{{dsc see c | c/thread/call_once | once_flag}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}