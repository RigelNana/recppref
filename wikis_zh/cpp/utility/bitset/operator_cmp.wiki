{{cpp/utility/bitset/title|1=operator==|2=operator!=}}
{{cpp/utility/bitset/navbar}}
{{dcl begin}}
{{dcla|num=1|noexcept=c++11|constexpr=c++23|1=
bool operator==( const bitset&amp; rhs ) const;
}}
{{dcla|num=2|noexcept=c++11|until=c++20|1=
bool operator!=( const bitset&amp; rhs ) const;
}}
{{dcl end}}

@1@ 若 {{c|*this}} 与 {{c|rhs}} 中的所有位都相等则返回 {{c|true}}。

@2@ 若 {{c|*this}} 与 {{c|rhs}} 中有任何位不相等则返回 {{c|true}}。

{{rrev|since=c++20|
{{cpp/note synthesized eq}}
}}

===参数===
{{par begin}}
{{par|rhs|要比较的 {{tt|bitset}}}}
{{par end}} 

===返回值===
@1@ 若 {{c|*this}} 中每位都等于 {{c|rhs}} 中对应位的值则为 {{c|true}}，否则为 {{c|false}}。

@2@ 若 {{c|1=!(*this == rhs)}} 则为 {{c|true}}，否则为 {{c|false}}。

===示例===
{{example
|比较两个 {{tt|bitset}} 以确定它们是否等同：
|code=
#include &lt;bitset&gt;
#include &lt;iostream&gt;

int main()
{
    std::bitset&lt;4&gt; b1(0b0011);
    std::bitset&lt;4&gt; b2(b1);
    std::bitset&lt;4&gt; b3(0b0100);

    std::cout &lt;&lt; std::boolalpha;
    std::cout &lt;&lt; "b1 == b2: " &lt;&lt; (b1 == b2) &lt;&lt; '\n';
    std::cout &lt;&lt; "b1 == b3: " &lt;&lt; (b1 == b3) &lt;&lt; '\n';
    std::cout &lt;&lt; "b1 != b3: " &lt;&lt; (b1 != b3) &lt;&lt; '\n';

//  b1 == std::bitset&lt;3&gt;{}; // 编译时错误：不兼容类型
}
|output=
b1 == b2: true
b1 == b3: false
b1 != b3: true
}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}