{{cpp/utility/any/title|~any}}
{{cpp/utility/any/navbar}}
{{ddcl|since=c++17|
~any();
}}
若存在则销毁所含对象，如同通过调用 {{rlpf|reset()}} 进行。

===示例===
{{example
|code=
#include &lt;any&gt;
#include &lt;cstdio&gt;

struct X
{
    X() { std::puts("X::X()"); }
    X(const X&amp;) { std::puts("X::X(const X&amp;)"); }
    ~X() { std::puts("X::~X()"); }
};

int main()
{
    std::any a{X{}&lt;!----&gt;};
    std::puts("Leaving main()...");
}
|output=
X::X()
X::X(const X&amp;)
X::~X()
Leaving main()...
X::~X()
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/utility/any/dsc reset}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}