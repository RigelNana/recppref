{{cpp/utility/initializer_list/title|end}}
{{cpp/utility/initializer_list/navbar}}
{{ddcla|since=c++11|constexpr=c++14|
const T* end() const noexcept;
}}

获得指向初始化器列表末元素后一位置的指针，即 {{box|{{rlpf|begin}}{{nbspt}}{{c/core|+ size()}}}}。

若初始化器列表为空，则 {{rlpf|begin}} 与 {{tt|end()}} 的值是未指定的，但一定相同。

===参数===
（无）

===返回值===
指向初始化器列表末元素后一位置的指针

===复杂度===
常数

===示例===
{{example
|code=
#include &lt;initializer_list&gt;
#include &lt;numeric&gt;

int main()
{
    static constexpr auto l = {3, 13, 13};
    static_assert(std::accumulate(l.begin(), l.end(), 13) == 42);
}
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/utility/initializer_list/dsc begin}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}