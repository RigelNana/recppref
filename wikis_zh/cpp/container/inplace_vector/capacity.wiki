{{cpp/container/inplace_vector/title|capacity}}
{{cpp/container/inplace_vector/navbar}}
{{dcl begin}}
{{dcl|since=c++26|
static constexpr size_type capacity() noexcept;
}}
{{dcl end}}

返回内部（原位）存储的容量。等价于：{{c|return N;}}。

===参数===
（无）

===返回值===
容器能够保有元素的最大数量。

===复杂度===
常数。

===注解===
因为每个 {{c/core|std::inplace_vector&lt;T, N&gt;}} 都是固定容量的容器，所以 {{tt|capacity}} 返回的值都等于 {{tt|N}}（这也是 {{lc|max_size()}} 返回的值）。

===示例===
{{example
|code=
#include &lt;inplace_vector&gt;

int main()
{
    constexpr std::inplace_vector&lt;int, 4&gt; v1;
    static_assert(v1.capacity() == 4 &amp;&amp; v1.max_size() == 4);

    constexpr std::inplace_vector&lt;int, 0&gt; v2;
    static_assert(v2.capacity() == 0 &amp;&amp; v2.max_size() == 0);
}
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/container/dsc max_size|inplace_vector}}
{{dsc inc|cpp/container/dsc size|inplace_vector}}
{{dsc inc|cpp/container/dsc resize|inplace_vector}}
{{dsc inc|cpp/container/dsc empty|inplace_vector}}
{{dsc inc|cpp/container/dsc reserve|inplace_vector}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pl|pt|ru}}