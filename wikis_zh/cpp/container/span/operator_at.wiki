{{cpp/container/span/title|operator[]}}
{{cpp/container/span/navbar}}
{{ddcl|since=c++20|
constexpr reference operator[]( size_type idx ) const;
}}

获得到序列的第 {{c|idx}} 个元素的引用。

{{cpp/hardened ub if|since=c++26| {{c|idx &lt; size()}} 是 {{c|false}}}}

===参数===
{{par begin}}
{{par|idx|要访问的元素的索引}}
{{par end}}

===返回值===
{{c|data()[idx]}}

===异常===
不抛出。

===示例===
{{example|code=
#include &lt;cstddef&gt;
#include &lt;iostream&gt;
#include &lt;span&gt;
#include &lt;utility&gt;

void reverse(std::span&lt;int&gt; span)
{
    for (std::size_t i = 0, j = std::size(span); i &lt; j; ++i)
    {
        --j;
        std::swap(span[i], span[j]);
    }
}

void print(const std::span&lt;const int&gt; span)
{
    for (int element : span)
        std::cout &lt;&lt; element &lt;&lt; ' ';
    std::cout &lt;&lt; '\n';
}

int main()
{
    int data[]{1, 2, 3, 4, 5};
    print(data);
    reverse(data);
    print(data);
}
|output=
1 2 3 4 5
5 4 3 2 1
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/container/dsc at|span}}
{{dsc inc|cpp/container/dsc data|span}}
{{dsc inc|cpp/container/dsc size|span}}
{{dsc inc|cpp/container/span/dsc as_bytes}}
{{dsc end}}

{{langlinks|de|en|es|ja|ru}}