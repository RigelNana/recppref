{{cpp/container/span/title|empty}}
{{cpp/container/span/navbar}}
{{ddcl|since=c++20|
constexpr bool empty() const noexcept;
}}

检查 span 是否为空。等价于 {{c|1=return size() == 0;}}。

===返回值===
span 为空时返回 {{c|true}}；否则返回 {{c|false}}。

===示例===
{{example|code=
#include &lt;iomanip&gt;
#include &lt;iostream&gt;
#include &lt;span&gt;
 
int main()
{
    std::span&lt;const char&gt; span{"ABCDEF"};
    
    while (!span.empty())
    {
        std::cout &lt;&lt; std::quoted(span.data()) &lt;&lt; '\n';
        span = span.last(span.size() - 1);
    }
}
|output=
"ABCDEF"
"BCDEF"
"CDEF"
"DEF"
"EF"
"F"
""
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/container/span/dsc size}}
{{dsc end}}

{{langlinks|de|en|es|ja|pl|ru}}