{{cpp/chrono/duration/title|count}}
{{cpp/chrono/duration/navbar}}
{{ddcl|since=c++11|
constexpr Rep count() const;
}}

返回此 duration 的计次数。

===参数===
（无）

===返回值===
此 duration 的计次数。

===示例===
{{example 
|code=
#include &lt;chrono&gt;
#include &lt;iostream&gt;

int main()
{
    std::chrono::milliseconds ms{3}; // 3 毫秒
    // 从 3 毫秒构造 6000 微秒
    std::chrono::microseconds us = 2 * ms;
    // 使用分数计次的 30Hz 时钟
    std::chrono::duration&lt;double, std::ratio&lt;1, 30&gt;&gt; hz30(3.5);

    std::cout &lt;&lt; "3 ms 时长有 " &lt;&lt; ms.count() &lt;&lt; " 计次\n"
              &lt;&lt; "6000 us 时长有 " &lt;&lt; us.count() &lt;&lt; " 计次\n"
              &lt;&lt; "3.5 30Hz 时长有 " &lt;&lt; hz30.count() &lt;&lt; " 计次\n";       
}
|output=
3 ms 时长有 3 计次
6000 us 时长有 6000 计次
3.5 30Hz 时长有 3.5 计次
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/chrono/duration/dsc duration_cast}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}