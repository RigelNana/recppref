{{cpp/title|n=literals::chrono_literals::|operator""min}}
{{cpp/chrono/duration/navbar}}
{{dcl begin}}
{{dcl header|chrono}}
{{dcl|num=1|since=c++14|
constexpr chrono::minutes
    operator""min( unsigned long long mins );
}}
{{dcl|num=2|since=c++14|
constexpr chrono::duration&lt;/* 未指明 */, ratio&lt;60,1&gt;&gt;
    operator""min( long double mins );
}}
{{dcl end}}

组成表示分的 {{lc|std::chrono::duration}} 字面量。

@1@ 整数字面量，准确返回 {{c|std::chrono::minutes(mins)}}。
@2@ 浮点字面量，返回等价于 {{lc|std::chrono::minutes}} 的浮点时长。

===参数===
{{par begin}}
{{par | minutes |分钟数}}
{{par end}}

===返回值===
{{lc|std::chrono::duration}} 字面量。

===可能的实现===
{{eq fun
|1=
constexpr std::chrono::minutes operator""min(unsigned long long m)
{
    return std::chrono::minutes(m);
}
constexpr std::chrono::duration&lt;long double,
                                std::ratio&lt;60,1&gt;&gt; operator""min(long double m)
{
    return std::chrono::duration&lt;long double, ratio&lt;60,1&gt;&gt; (m);
}
}}

===注意===
{{cpp/chrono/chrono_literals_namespace}}

===示例===
{{example
|code=
#include &lt;chrono&gt;
#include &lt;iostream&gt;

int main()
{
    using namespace std::chrono_literals;
    auto lesson = 45min;
    auto halfmin = 0.5min;
    std::cout &lt;&lt; "一堂课有 " &lt;&lt; lesson.count() &lt;&lt; " 分"
                 " (" &lt;&lt; lesson &lt;&lt; ")\n"
              &lt;&lt; "半分钟有 " &lt;&lt; halfmin.count() &lt;&lt; " 分"
                 " (" &lt;&lt; halfmin &lt;&lt; ")\n";
}
|output=
一堂课有 45 分 (45min)
半分钟有 0.5 分 (0.5min)
}}

===参阅===
{{dsc begin}}
{{dsc inc | cpp/chrono/duration/dsc constructor}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}