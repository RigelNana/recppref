{{cpp/chrono/system_clock/title|from_time_t}}
{{cpp/chrono/system_clock/navbar}}
{{ddcl|since=c++11|
static std::chrono::system_clock::time_point from_time_t( std::time_t t ) noexcept;
}}

用两个类型间的较低精度，转换 {{c|t}} 为时间点类型。

若 {{tt|time_point}} 拥有较低精度，则舍入还是截断值是实现定义的。

===参数===
{{par begin}}
{{par | t |要转换的 {{lc|std::time_t}} 值}}
{{par end}}

===返回值===
表示 {{c|t}} 的 [[cpp/chrono/system_clock#成员类型|{{tt|std::chrono::system_clock::time_point}}]] 类型的值。

===示例===
{{example|code=
#include &lt;chrono&gt;
#include &lt;ctime&gt;
#include &lt;iostream&gt;
#include &lt;thread&gt;

int main()
{
    using namespace std::chrono_literals;

    const std::time_t t = std::time(nullptr); // 精度通常为 "1 秒"

    const auto from = std::chrono::system_clock::from_time_t(t);

    std::this_thread::sleep_for(500ms);

    const auto diff = std::chrono::system_clock::now() - from;

    std::cout &lt;&lt; diff &lt;&lt; " ("
              &lt;&lt; std::chrono::round&lt;std::chrono::milliseconds&gt;(diff)
              &lt;&lt; ")\n";
}
|p=true
|output=987654321ns (987ms)
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/chrono/system_clock/dsc to_time_t}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}