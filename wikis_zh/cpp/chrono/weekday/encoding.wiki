{{cpp/chrono/weekday/title|c_encoding|iso_encoding}}
{{cpp/chrono/weekday/navbar}}
{{dcl begin}}
{{dcl|num=1|since=c++20|1=
constexpr unsigned c_encoding() const noexcept;
}}
{{dcl|num=2|since=c++20|1=
constexpr unsigned iso_encoding() const noexcept;
}}
{{dcl end}}

@1@ 返回存储于 {{c|*this}} 的星期之日值。
@2@ 返回存储于 {{c|*this}} 的星期之日值，但按照 ISO 8601 将星期日（存储为 {{c|0}}）为判读为 {{c|7}}。

=== 返回值 ===
@1@ 存储于 {{tt|*this}} 的星期之日值。
@2@ {{c|1=c_encoding() == 0u ? 7u : c_encoding()}}

===示例===
{{example
|code=
#include &lt;chrono&gt;
#include &lt;iostream&gt;

int main()
{
    std::cout &lt;&lt; "i: C: ISO: Weekday:\n";

    for (unsigned i{0}; i != 8; ++i)
    {
        const std::chrono::weekday w{i};
        std::cout &lt;&lt; i &lt;&lt; "  "
                  &lt;&lt; w.c_encoding() &lt;&lt; "  "
                  &lt;&lt; w.iso_encoding() &lt;&lt; "    "
                  &lt;&lt; w &lt;&lt; '\n';
    }
}
|output=
i: C: ISO: Weekday:
0  0  7    Sun
1  1  1    Mon
2  2  2    Tue
3  3  3    Wed
4  4  4    Thu
5  5  5    Fri
6  6  6    Sat
7  0  7    Sun
}}

{{langlinks|de|en|es|fr|ja|ru}}