{{cpp/chrono/month_day_last/title|month}}
{{cpp/chrono/month_day_last/navbar}}
{{dcl begin}}
{{dcl|since=c++20|1=
constexpr std::chrono::month month() const noexcept;
}}
{{dcl end}}

取得存储于 {{tt|*this}} 的 {{lc|std::chrono::month}} 对象的副本。

=== 返回值 ===
存储于 {{tt|*this}} 的 {{lc|std::chrono::month}} 对象的副本。

===示例===
{{example
|code=
#include &lt;chrono&gt;
#include &lt;iostream&gt;

using namespace std::chrono;

int main()
{
    std::cout &lt;&lt; std::boolalpha;

    auto mdl{February/last}; // 二月最后一天
    auto ymdl{year(2020) / mdl};
    std::cout &lt;&lt; (year_month_day{ymdl} == year_month_day{February/29/2020}) &lt;&lt; ' ';

    mdl = (mdl.month() + months(1)) / last; // 2020 年中下一个月的最后一天
    ymdl = year(2020) / mdl;
    std::cout &lt;&lt; (year_month_day{ymdl} == year_month_day{March/31/2020}) &lt;&lt; '\n';
}
|output=
true true
}}

{{langlinks|en|es|ja|ru}}