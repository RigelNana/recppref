{{cpp/chrono/title|operator&lt;&lt;{{small|(std::chrono::month)}}}}
{{cpp/chrono/month/navbar}}
{{ddcl|header=chrono|since=c++20|
template&lt; class CharT, class Traits &gt;
std::basic_ostream&lt;CharT, Traits&gt;&amp;
    operator&lt;&lt;( std::basic_ostream&lt;CharT, Traits&gt;&amp; os, const std::chrono::month&amp; m );
}}

若 {{c|!m.ok()}}，则将 {{c|unsigned(m)}} 后随 {{c|" is not a valid month"}} 插入到 {{c|os}}。否则，构成一个 {{c|std::basic_string&lt;CharT&gt;}} {{c|s}}，它由以与 {{c|os}} 关联的本地环境所确定的 {{c|m}} 所表示的月的缩写月名组成，并将 {{c|s}} 插入到 {{c|os}} 中。

等价于

{{cc|
return os &lt;&lt; (m.ok() ?
    std::format(os.getloc(), STATICALLY_WIDEN&lt;CharT&gt;("{:L%b}"), m) :
    std::format(os.getloc(), STATICALLY_WIDEN&lt;CharT&gt;("{} is not a valid month"), unsigned(m)));
}}

其中 {{c|STATICALLY_WIDEN&lt;CharT&gt;("...")}} 当 {{tt|CharT}} 为 {{c|char}} 时为 {{c|"..."}}，而当 {{tt|CharT}} 为 {{c|wchar_t}} 时为 {{c|L"..."}}。

===返回值===
{{c|os}}

===注解===
此 {{c|operator&lt;&lt;}} 主要为调试用途而设，对于格式化上的控制，请用 {{lc|std::format}}。

===缺陷报告===
{{dr list begin}}
{{dr list item|paper=P2372R3|std=C++20|before=默认使用给定的本地环境|after=需要 {{ttb|L}} 以使用本地环境}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/utility/format/dsc format}}
{{dsc inc|cpp/chrono/dsc formatter|month}}
{{dsc end}}

{{langlinks|de|es|en|ja|ru}}