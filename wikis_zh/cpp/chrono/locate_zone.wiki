{{cpp/chrono/title|locate_zone}}
{{cpp/chrono/navbar}}
{{ddcl|since=c++20|header=chrono|
const std::chrono::time_zone* locate_zone( std::string_view tz_name );
}}

在{{enwiki|List of tz database time zones|时区数据库}}中定位时区的便利函数。等价于 {{c|std::chrono::get_tzdb().locate_zone(tz_name)}}。

===参数===
{{par begin}}
{{par|tz_name|要定位的时区名称}}
{{par end}}

=== 异常 ===
若找不到指定的时区，或若这次调用是对时区数据库的首次引用且无法初始化时区数据库，则抛出 {{lc|std::runtime_error}}。

=== 注解 ===
若对此函数的调用是对时区数据库的首次引用，则它将导致初始化时区数据库。

===示例===
{{example
|code=
#include &lt;chrono&gt;
#include &lt;iostream&gt;

int main()
{
    try
    {
        for (const auto zone : {"Atlantic/Bermuda", "Alcatraz"})
            std::cout &lt;&lt; std::chrono::locate_zone(zone)-&gt;name() &lt;&lt; '\n';
    }
    catch(const std::runtime_error&amp; ex)
    {
        std::cout &lt;&lt; ex.what() &lt;&lt; '\n';
    }
}
|p=true
|output=
Atlantic/Bermuda
std::chrono::tzdb: cannot locate zone: Alcatraz
}}

=== 参阅 ===
{{dsc begin}}
{{dsc inc|cpp/chrono/tzdb/dsc locate_zone}}
{{dsc inc|cpp/chrono/dsc tzdb functions}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}