{{cpp/title|n=literals::chrono_literals::|operator""ms}}
{{cpp/chrono/duration/navbar}}
{{dcl begin}}
{{dcl header|chrono}}
{{dcl|num=1|since=c++14|
constexpr std::chrono::milliseconds
    operator""ms( unsigned long long ms );
}}
{{dcl|num=2|since=c++14|
constexpr std::chrono::duration&lt;/* 未指明 */, std::milli&gt;
    operator""ms( long double ms );
}}
{{dcl end}}

组成表示毫秒的 {{lc|std::chrono::duration}} 字面量。

@1@ 整数字面量，准确返回 {{c|std::chrono::milliseconds(ms)}}。 
@2@ 浮点字面量，返回等价于 {{lc|std::chrono::milliseconds}} 的浮点时长。

===参数===
{{par begin}}
{{par | ms |毫秒数}}
{{par end}}

===返回值===
{{lc|std::chrono::duration}} 字面量。

===可能的实现===
{{eq fun
|1=
constexpr std::chrono::milliseconds operator""ms(unsigned long long ms)
{
    return std::chrono::milliseconds(ms);
}
constexpr std::chrono::duration&lt;long double, std::milli&gt; operator""ms(long double ms)
{
    return std::chrono::duration&lt;long double, std::milli&gt;(ms);
}
}}

===注解===
{{cpp/chrono/chrono_literals_namespace}}

===示例===
{{example
|code=
#include &lt;chrono&gt;
#include &lt;iostream&gt;

int main()
{
    using namespace std::chrono_literals;
    constexpr auto d1{250ms};
    constexpr std::chrono::milliseconds d2{1s};
    std::cout &lt;&lt; d1 &lt;&lt; " = " &lt;&lt; d1.count() &lt;&lt; " 毫秒\n"
              &lt;&lt; d2 &lt;&lt; " = " &lt;&lt; d2.count() &lt;&lt; " 毫秒\n";
}
|output=
250ms = 250 毫秒
1000ms = 1000 毫秒
}}

===参阅===
{{dsc begin}}
{{dsc inc | cpp/chrono/duration/dsc constructor}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}