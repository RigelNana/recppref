{{cpp/chrono/month_day/title|month|day}}
{{cpp/chrono/month_day/navbar}}
{{dcl begin}}
{{dcl|since=c++20|num=1|1=
constexpr std::chrono::month month() const noexcept;
}}
{{dcl|since=c++20|num=1|1=
constexpr std::chrono::day day() const noexcept;
}}
{{dcl end}}

取得存储于此 {{tt|month_day}} 对象的月和日期值。

===返回值===
@1@ 返回存储的 {{lc|std::chrono::month}} 值。
@2@ 返回存储的 {{lc|std::chrono::day}} 值。

===示例===
{{example
|code=
#include &lt;chrono&gt;

int main()
{
    constexpr auto md{std::chrono::July/15};  
    static_assert(md.month() == std::chrono::month(7));
    static_assert(md.day() == std::chrono::day(15));
}
}}

{{langlinks|en|es|ja|ru}}