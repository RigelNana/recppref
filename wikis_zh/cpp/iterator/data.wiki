{{cpp/title|data}}
{{cpp/iterator/navbar}}
{{dcl begin}}
{{cpp/iterator/range access headers}}
{{dcla|num=1|since=c++17|
template&lt; class C &gt;
constexpr auto data( C&amp; c ) -&gt; decltype(c.data());
}}
{{dcla|num=2|since=c++17|
template&lt; class C &gt;
constexpr auto data( const C&amp; c ) -&gt; decltype(c.data());
}}
{{dcla|num=3|since=c++17|
template&lt; class T, std::size_t N &gt;
constexpr T* data( T (&amp;array)[N] ) noexcept;
}}
{{dcla|num=4|since=c++17|
template&lt; class E &gt;
constexpr const E* data( std::initializer_list&lt;E&gt; il ) noexcept;
}}
{{dcl end}}

返回指向含有范围元素的内存块的指针。

@1,2@ 返回 {{c|c.data()}}。
@3@ 返回 {{c|array}}。
@4@ 返回 {{c|il.begin()}}。

===参数===
{{par begin}}
{{par|c|有 {{c|data()}} 方法的容器或视图}}
{{par|array|任意类型的数组}}
{{par|il|一个 {{lc|std::initializer_list}}}}
{{par end}}

===返回值===
@1,2@ {{c|c.data()}}
@3@ {{c|array}}
@4@ {{c|il.begin()}}

===异常===
@1@ {{cpp/impldef exception item}}

===注解===
需要对 {{lc|std::initializer_list}} 的重载，因为它没有成员函数 {{tt|data}}。

{{feature test macro|__cpp_lib_nonmember_container_access|{{lc|std::size()}}, {{tt|std::data()}}, 和 {{lc|std::empty()}}|value=201411L|std=C++17}}

===可能的实现===
{{eq impl
|ver1=1|1=
template&lt;class C&gt;
constexpr auto data(C&amp; c) -&gt; decltype(c.data())
{
    return c.data();
}
|ver2=2|2=
template&lt;class C&gt;
constexpr auto data(const C&amp; c) -&gt; decltype(c.data())
{
    return c.data();
}
|ver3=3|3=
template&lt;class T, std::size_t N&gt;
constexpr T* data(T (&amp;array)[N]) noexcept
{
    return array;
}
|ver4=4|4=
template&lt;class E&gt;
constexpr const E* data(std::initializer_list&lt;E&gt; il) noexcept
{
    return il.begin();
}
}}

===示例===
{{example
|code=
#include &lt;cstring&gt;
#include &lt;iostream&gt;
#include &lt;string&gt;

int main()
{
    std::string s{"Hello world!\n"};

    char a[20]; // C-style 风格字符串的存储
    std::strcpy(a, std::data(s));
//  C++11 起 [s.data(), s.data() + s.size()] 保证为 NTBS

    std::cout &lt;&lt; a;
}
|output=
Hello world!
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/dsc data}}
{{dsc inc|cpp/ranges/dsc cdata}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}