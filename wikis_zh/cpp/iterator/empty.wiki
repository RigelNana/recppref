{{cpp/title|empty}}
{{cpp/iterator/navbar}}
{{dcl begin}}
{{cpp/iterator/range access headers}}
{{dcla|num=1|since=c++17|
template&lt; class C &gt;
constexpr auto empty( const C&amp; c ) -&gt; decltype(c.empty());
}}
{{dcla|num=2|since=c++17|
template&lt; class T, std::size_t N &gt;
constexpr bool empty( const T (&amp;array)[N] ) noexcept;
}}
{{dcla|num=3|since=c++17|
template&lt; class E &gt;
constexpr bool empty( std::initializer_list&lt;E&gt; il ) noexcept;
}}
{{dcl end}}

返回给定的范围是否为空。

@1@ 返回 {{c|c.empty()}}。
@2@ 返回 {{c|false}}。
@3@ 返回 {{c|1=il.size() == 0}}。

===参数===
{{par begin}}
{{par|c|拥有 {{tt|empty}} 成员函数的容器或视图}}
{{par|array|任意类型的数组}}
{{par|il|一个 {{lc|std::initializer_list}}}}
{{par end}}

===返回值===
@1@ {{c|c.empty()}}
@2@ {{c|false}}
@3@ {{c|1=il.size() == 0}}

===异常===
@1@ {{cpp/impldef exception item}}

===注解===
需要对 {{lc|std::initializer_list}} 的重载，因为它没有成员函数 {{tt|empty}}。

{{feature test macro|__cpp_lib_nonmember_container_access|{{lc|std::size()}}, {{lc|std::data()}}, 和 {{tt|std::empty()}}|value=201411L|std=C++17}}

===可能的实现===
{{eq impl
|ver1=1|1=
template&lt;class C&gt;
[[nodiscard]] constexpr auto empty(const C&amp; c) -&gt; decltype(c.empty())
{
    return c.empty();
}
|ver2=2|2=
template&lt;class T, std::size_t N&gt;
[[nodiscard]] constexpr bool empty(const T (&amp;array)[N]) noexcept
{
    return false;
}
|ver3=3|3=
template&lt;class E&gt;
[[nodiscard]] constexpr bool empty(std::initializer_list&lt;E&gt; il) noexcept
{
    return il.size() == 0;
}
}}

===示例===
{{example
|code=
#include &lt;iostream&gt;
#include &lt;vector&gt;

template&lt;class T&gt;
void print(const T&amp; container)
{
    if (std::empty(container))
        std::cout &lt;&lt; "空\n";
    else
    {
        std::cout &lt;&lt; "元素：";
        for (const auto&amp; element : container)
            std::cout &lt;&lt; ' ' &lt;&lt; element;
        std::cout &lt;&lt; '\n';
    }
}

int main()
{
    std::vector&lt;int&gt; c = {1, 2, 3};
    print(c);
    c.clear();
    print(c);
    
    int array[] = {4, 5, 6};
    print(array);
    
    auto il = {7, 8, 9};
    print(il);
}
|output=
元素： 1 2 3
空
元素： 4 5 6
元素： 7 8 9
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/ranges/dsc empty}}
{{dsc end}}

{{langlinks|en|es|ja|ru}}