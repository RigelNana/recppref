{{cpp/regex/match_results/title|prefix}}
{{cpp/regex/match_results/navbar}}
{{ddcl|since=c++11|
const_reference prefix() const;
}}

获得到 {{lc|std::sub_match}} 对象的引用，该对象表示目标序列起始到正则表达式的整个匹配起始之间的目标序列部分。

{{cpp/precondition|{{rlpf|ready}} 必须为 {{c|true}}。}}

===返回值===
到未匹配前缀的引用。

===示例===
{{example
|code=
#include &lt;iostream&gt;
#include &lt;regex&gt;
#include &lt;string&gt;

int main()
{
    std::regex re("a(a)*b");
    std::string target("baaaby");
    std::smatch sm;

    std::regex_search(target, sm, re);
    std::cout &lt;&lt; sm.prefix().str() &lt;&lt; '\n';
}
|output=
b
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/regex/match_results/dsc suffix}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}