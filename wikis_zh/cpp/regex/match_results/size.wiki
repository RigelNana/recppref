{{cpp/regex/match_results/title|size}}
{{cpp/regex/match_results/navbar}}
{{ddcl|since=c++11|
size_type size() const noexcept;
}}

返回子匹配个数，即 {{c|std::distance(begin(), end())}}。

若 {{c|*this}} 不表示成功匹配的结果则返回 {{c|0}}。

===参数===
（无）

===返回值===
子匹配个数。

===复杂度===
常数。

===示例===
{{example
|code=
#include &lt;iostream&gt;
#include &lt;regex&gt;
#include &lt;string&gt;

int main()
{
    std::regex re("a(a)*b");
    std::string target("aaab");
    std::smatch sm;

    std::cout &lt;&lt; sm.size() &lt;&lt; '\n';

    std::regex_match(target, sm, re);
    std::cout &lt;&lt; sm.size() &lt;&lt; '\n';
}
|output=
0
2
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/regex/match_results/dsc begin}}
{{dsc inc|cpp/regex/match_results/dsc end}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}