{{title|{{tt|sizeof...}} 运算符 {{mark since c++11}}}}
{{cpp/language/expressions/templates/navbar}}
查询{{rlp|pack|包}}中的元素数量。

===语法===
{{sdsc begin}}
{{sdsc|{{ttb|sizeof...(}} {{spar|包}} {{ttb|)}}}}
{{sdsc end}}

返回 {{lc|std::size_t}} 类型的常量。

===解释===
返回{{rlp|pack|包}}中的元素数量。

===关键词===
{{ltt|cpp/keyword/sizeof}}

===示例===
{{example
|code=
#include &lt;array&gt;
#include &lt;iostream&gt;
#include &lt;type_traits&gt;
 
template&lt;typename... Ts&gt;
constexpr auto make_array(Ts&amp;&amp;... ts)
{
    using CT = std::common_type_t&lt;Ts...&gt;;
    return std::array&lt;CT, sizeof...(Ts)&gt;{std::forward&lt;CT&gt;(ts)...};
}
 
int main()
{
    std::array&lt;double, 4ul&gt; arr = make_array(1, 2.71f, 3.14, '*');
    std::cout &lt;&lt; "arr = { ";
    for (auto s{arr.size()}; double elem : arr)
        std::cout &lt;&lt; elem &lt;&lt; (--s ? ", " : " ");
    std::cout &lt;&lt; "}\n";
}
|output=arr = { 1, 2.71, 3.14, 42 }
}}

===参阅===
* {{rlp|sizeof}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}