{{cpp/keyword/title|{{tt|xor}}}}
{{cpp/keyword/navbar}}

===用法===
* [[cpp/language/operator alternative |代用运算符]]：作为 {{tt|^}} 的替代写法

===示例===
{{example
|code=
#include &lt;bitset&gt;
#include &lt;iostream&gt;

using bin = std::bitset&lt;8&gt;;

void show(bin z, const char* s, int n)
{
    if (n == 0) std::cout &lt;&lt; "┌─────────┬──────────┐\n";
    if (n &lt;= 2) std::cout &lt;&lt; "│ "&lt;&lt;s&lt;&lt;" │ " &lt;&lt;z&lt;&lt;" │\n";
    if (n == 2) std::cout &lt;&lt; "└─────────┴──────────┘\n";
}

int main()
{
    bin x{"01011010"}; show(x, "x      ", 0);
    bin y{"00111100"}; show(y, "y      ", 1);
    bin z = x xor y;   show(z, "x xor y", 2);
}
|output=
┌─────────┬──────────┐
│ x       │ 01011010 │
│ y       │ 00111100 │
│ x xor y │ 01100110 │
└─────────┴──────────┘
}}

===参阅===
{{cpp/keyword/alt operators list}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}