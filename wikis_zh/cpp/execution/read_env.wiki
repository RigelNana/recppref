{{cpp/execution/title|read_env}}
{{cpp/execution/navbar}}
{{dcl begin}}
{{dcl header|execution}}
{{dcl|since=c++26|notes={{mark custpt}}|1=
inline constexpr /* 未指明 */ read_env{};
}}
{{dcl h|调用签名}}
{{dcl|since=c++26|1=
execution::sender auto read_env( auto&amp;&amp; query );
}}
{{dcl end}}

发送器工厂，它返回的发送器联系接收器的环境，并拉出与给定[[cpp/execution#查询|查询对象]]关联的当前值。

对于任意查询对象 {{tt|q}}，表达式 {{c|read_env(q)}} [[cpp/language/expressions#表达式等价|表达式等价]]于 {{c|/*make-sender*/(read_env, q)}}。

{{cpp/execution/cpo|tag=no}}

===示例===
这个工厂的一个用法示例，是在接收器的调度器上调度待决的工作，可以通过 {{c|read_env(get_scheduler)}} 获得调度器：
{{source|1=
std::execution::sender auto task =
  std::execution::read_env(std::execution::get_scheduler)
    {{!}} std::execution::let_value([](auto sched) {
        return std::execution::starts_on(sched, /* 这里是一些嵌套的工作 */);
    });

std::this_thread::sync_wait( std::move(task) ); // 等待它完成
}}

{{langlinks|de|en|es|ja|ru}}