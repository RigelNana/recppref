{{cpp/io/ios_base/title|getloc}}
{{cpp/io/ios_base/navbar}}
{{ddcl|
std::locale getloc() const;
}}

返回当前与流关联的本地环境。

===参数===
（无）

===返回值===
与流关联的本地环境对象。

===示例===
{{example
|给出的输出是用 clang 编译器取得的。
|code=
#include &lt;codecvt&gt;
#include &lt;ctime&gt;
#include &lt;iomanip&gt;
#include &lt;iostream&gt;

int main()
{
    std::wbuffer_convert&lt;std::codecvt_utf8&lt;wchar_t&gt;&gt; conv(std::cout.rdbuf());
    std::wostream out(&amp;conv);
    
    out.imbue(std::locale(out.getloc(),
                          new std::time_put_byname&lt;wchar_t&gt;("ja_JP.utf8")));
    
    std::time_t t = std::time(nullptr);
    out &lt;&lt; std::put_time(std::localtime(&amp;t), L"%A %c") &lt;&lt; '\n';
}
|p=true
|output=
木曜日 2023年10月05日 19時47分58秒
}}

===缺陷报告===
{{dr list begin}}
{{dr list item|wg=lwg|dr=47|std=C++98|before=返回值被错误地指定为 {{tt|imbue()}} 的返回值|after=已改正}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/io/ios_base/dsc imbue}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}