{{cpp/title|putwchar}}
{{cpp/io/c/navbar}}
{{ddcl|header=cwchar|
std::wint_t putwchar( wchar_t ch );
}}
写入宽字符 {{c|ch}} 到 {{tt|stdout}}。

===参数===
{{par begin}}
{{par|ch|要写入的宽字符}}
{{par end}}

===返回值===
成功时为 {{c|ch}}，失败时为 {{c|WEOF}}。

===示例===
{{example
|code=
#include &lt;clocale&gt;
#include &lt;cstdio&gt;
#include &lt;cstdlib&gt;
#include &lt;cwchar&gt;
#include &lt;initializer_list&gt;

int main()
{
    std::setlocale(LC_ALL, "en_US.utf8");

    for (const wchar_t ch : {
        L'\u2200', // Unicode 名: "FOR ALL"
        L'∀',
        L'\n'
        })
        if (std::putwchar(ch) == WEOF)
        {
            std::puts("I/O error in std::putwchar");
            return EXIT_FAILURE;
        }

    return EXIT_SUCCESS;
}
|p=true
|output=∀∀
}}

===返回值===
{{dsc begin}}
{{dsc inc|cpp/io/c/dsc putchar}}
{{dsc inc|cpp/io/c/dsc fputwc}}
{{dsc see c|c/io/putwchar}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}