{{cpp/io/basic_filebuf/title|swap}}
{{cpp/io/basic_filebuf/navbar}}
{{dcl begin}}
{{dcl|since=c++11|
void swap( std::basic_filebuf&amp; rhs );
}}
{{dcl end}}

交换 {{c|*this}} 与 {{c|rhs}} 的状态和内容。

===参数===
{{par begin}}
{{par | rhs |另一 {{tt|basic_filebuf}} }}
{{par end}}

===返回值===
（无）

===注解===
在交换 {{lc|std::fstream}} 对象时自动调用此函数，很少需要直接调用它。

===示例===
{{example
|code=
#include &lt;fstream&gt;
#include &lt;iostream&gt;
#include &lt;string&gt;
 
int main()
{
    std::ifstream fin("test.in"); // 只读
    std::ofstream fout("test.out"); // 只写

    std::string s;
    getline(fin, s);
    std::cout &lt;&lt; s &lt;&lt; '\n'; // 输出 test.in 的第一行

    fin.rdbuf()-&gt;swap(*fout.rdbuf()); // 交换底层缓冲

    getline(fin, s); // 失败：不能从只写 filebuf 读取
    std::cout &lt;&lt; s &lt;&lt; '\n'; // 打印空行
}
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/io/basic_filebuf/dsc operator{{=}}}}
{{dsc inc|cpp/io/basic_filebuf/dsc swap2}}
{{dsc inc|cpp/io/basic_fstream/dsc swap|basic_fstream}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}