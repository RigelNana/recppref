{{cpp/title|print{{small|(std::ostream)}}}}
{{cpp/io/basic_ostream/navbar}}
{{ddcl|header=ostream|since=c++23|
template&lt; class... Args &gt;
  void print( std::ostream&amp; os, std::format_string&lt;Args...&gt; fmt, Args&amp;&amp;... args );
}}

根据格式字符串 {{c|fmt}} 格式化 {{c|args}}，将结果插入流 {{c|os}}。

@@ 如果[[cpp/language/charset#代码单元与字面量编码|通常字面量编码]]是 UTF-8，则上式等价于：
* {{box|{{ltt|cpp/io/basic_ostream/vprint_unicode|std::vprint_unicode}}{{c/core|(os, fmt.get(), std::make_format_args(args...));}}}}。
* 否则等价于 {{box|{{ltt|cpp/io/basic_ostream/vprint_nonunicode|std::vprint_nonunicode}}{{c/core|(os, fmt.get(), std::make_format_args(args...));}}}}。

若 {{tt|Args}} 中有任何 {{tt|Ti}}，其 {{c/core|std::formatter&lt;Ti, char&gt;}} 不满足{{named req|BasicFormatter}}，则其行为未定义（正如 {{c|std::make_format_args}} 所要求的）。

===参数===
{{par begin}}
{{par|os|要插入数据的输出流}}
{{par|fmt|{{include|cpp/utility/format/format string}}}}
{{par|args...|参与格式化的实参}}
{{par end}}

===异常===
{{std print ostream exceptions}}

===注解===
{{ftm begin|sort=yes}}
{{ftm|std=C++23|value=202207L|__cpp_lib_print|格式化输出}}
{{ftm|std=C++23|value=202207L|__cpp_lib_format|暴露 {{lc|std::basic_format_string}}}}
{{ftm end}}

===示例===
{{example
|code=
#include &lt;array&gt;
#include &lt;cctype&gt;
#include &lt;cstdio&gt;
#include &lt;format&gt;
#include &lt;numbers&gt;
#include &lt;ranges&gt;
#include &lt;sstream&gt;

int main()
{
    std::array&lt;char, 24&gt; buf;
    std::format_to(buf.begin(), "{:.15f}", std::numbers::sqrt2);

    unsigned num{}, sum{};

     for (auto n : buf
                 {{!}} std::views::filter(isdigit)
                 {{!}} std::views::transform([](char x) { return x - '0'; })
                 {{!}} std::views::take_while([&amp;sum](char) { return sum &lt; 42; }))
        sum += n, ++num;

    std::stringstream stream;

#ifdef __cpp_lib_print
    std::print(stream,
#else
    stream &lt;&lt; std::format(
#endif
        "√2 \N{ALMOST EQUAL TO} {0}。\n"
        "其前 {1} 位的和是 {2}。",
        std::numbers::sqrt2, num, sum
    );

    std::puts(stream.str().data());
}
|output=
√2 ≈ 1.4142135623730951。
其前 13 位的和是 42。
}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/io/basic_ostream/dsc println}}
{{dsc inc|cpp/io/dsc print}}
{{dsc inc|cpp/utility/format/dsc format}}
{{dsc end}}

{{langlinks|de|en|es|fr|ja|ru}}