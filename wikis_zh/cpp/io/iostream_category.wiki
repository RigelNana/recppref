{{cpp/title|iostream_category}}
{{cpp/io/navbar}}
{{ddcl|header=ios|since=c++11|
const std::error_category&amp; iostream_category() noexcept;
}}

获取为 iostream 错误而设的静态错误类别对象的引用。要求对象覆写虚函数 {{c|error_category::name()}} 以返回指向字符串 {{c|"iostream"}} 的指针。它也用来鉴别由 {{lc|std::ios_base::failure}} 类型的异常提供的错误码。

===参数===
（无）

===返回值===
从 {{lc|std::error_category}} 派生的未指定运行时类型的静态对象的引用。

===示例===
{{example
|code=
#include &lt;fstream&gt;
#include &lt;iostream&gt;
 
int main()
{
    std::ifstream f("不存在");
    try
    {
        f.exceptions(f.failbit);
    }
    catch (const std::ios_base::failure&amp; e)
    {
        std::cout &lt;&lt; "捕获了 ios_base::failure。\n"
                  &lt;&lt; "错误码：" &lt;&lt; e.code().value() 
                  &lt;&lt; "（" &lt;&lt; e.code().message() &lt;&lt; "）\n"
                  &lt;&lt; "错误类别：" &lt;&lt; e.code().category().name() &lt;&lt; '\n';

    }
}
|p=true
|output=
捕获了 ios_base::failure。
错误码：1（unspecified iostream_category error）
错误类别：iostream
}}

===缺陷报告===
{{dr list begin}}
{{dr list item|wg=lwg|dr=2087|std=C++11|before={{tt|iostream_category}} 未声明为 {{c/core|noexcept}}|after=声明为 {{c/core|noexcept}}}}
{{dr list end}}

===参阅===
{{dsc begin}}
{{dsc inc|cpp/io/ios_base/dsc failure}}
{{dsc inc|cpp/io/dsc io_errc}}
{{dsc end}}

{{langlinks|de|en|es|fr|it|ja|pt|ru}}